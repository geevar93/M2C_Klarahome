<Project Sdk="Microsoft.NET.Sdk">

  <!--
    One-shot migration runner (docs/01-architecture.md §3.1). Runs as a job during deploy and
    exits non-zero on failure; the API never migrates on startup in production.

    Step 3 gives it the host and its exit-code contract. The EF Core migration application and
    idempotent-script generation are Step 4.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>KlaraHome.Migrator</RootNamespace>
    <AssemblyName>KlaraHome.Migrator</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="appsettings*.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../shared/KlaraHome.SharedKernel/KlaraHome.SharedKernel.csproj" />
    <ProjectReference Include="../../shared/KlaraHome.Infrastructure/KlaraHome.Infrastructure.csproj" />
  </ItemGroup>

</Project>
