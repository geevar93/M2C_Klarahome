{
  "name": "admin-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/admin-e2e/src",
  "implicitDependencies": ["admin"],
  "// targets": "to see all targets run: nx show project admin-e2e --web",
  "targets": {
    "visual-regression": {
      "executor": "nx:run-commands",
      "options": {
        "command": "playwright test --config=apps/admin-e2e/playwright.visual.config.mts",
        "cwd": "."
      }
    },
    "responsive-qa": {
      "executor": "nx:run-commands",
      "options": {
        "command": "playwright test --config=apps/admin-e2e/playwright.responsive.config.mts",
        "cwd": "."
      }
    }
  }
}
