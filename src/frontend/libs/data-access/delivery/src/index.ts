export * from './lib/delivery.service';

/** The shape this service answers with. Types only — see `data-access-content` for the reasoning. */
export type { ServiceabilityResponse } from '@klarahome/data-access-api';
