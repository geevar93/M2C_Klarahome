# Parking Lot — out-of-step discoveries

> Ledger for the Klara Home implementation plan.
> Tracker: [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) · Change log: [`CHANGE_LOG.md`](CHANGE_LOG.md) · Test debt: [`TEST_DEBT.md`](TEST_DEBT.md)

**Render this file when you are closing a step, or when the User asks what is outstanding.**
You do not need it while implementing.


Anything found mid-step that is **not** in that step's Deliverables goes here, and is discussed
with the User at the step boundary. Do not act on these items without explicit approval.

| Date | Raised during | Item | Decision |
|---|---|---|---|
| 2026-09-05 | Step 1 | **Node.js v20.12.2 is below Angular's supported range** (`^22.22.3 \|\| ^24.15.0 \|\| >=26.0.0`). Blocks Nx/Angular workspace generation. | ✅ **RESOLVED** — User upgraded to Node 24.20.0 via MSI; workspace generated and Step 1 closed |
| 2026-09-05 | Step 1 | Nx 23 templates inject AI-agent scaffolding (`.claude/`, `CLAUDE.md`, `.cursor/`, `.codex/`, `.gemini/`, `.opencode/`, `AGENTS.md`, `opencode.json`) into generated workspaces. Removed at generation time. | ℹ️ Removed. **Re-check after any Nx migration** — a `nx migrate` may reintroduce them |
| 2026-09-05 | Step 1 | `src/frontend/.gitignore` (Nx-generated) is separate from the root `.gitignore` and duplicates several rules. | ℹ️ Harmless; consolidate at Step 5 if it causes confusion |
| 2026-09-05 | Step 1 | Frontend `npm audit` reports **8 moderate** advisories in the fresh dependency tree, and 6 packages have uncovered install scripts. | ⏳ Not triaged — belongs to **Step 5** (vulnerability gate). Recorded so it is not missed |
| 2026-09-05 | Step 1 | `@angular-devkit/build-angular` warns that Angular's Webpack support is deprecated in favour of `@angular/build`. Our apps already use the esbuild-based builder. | ℹ️ No action; the package is a transitive leftover |
| 2026-09-05 | Step 1 | Legacy .NET SDKs 2.1.526 and 8.0.200 are also installed on this machine. Harmless — `global.json` pins 10.0.203 — but worth knowing if a build ever resolves unexpectedly. | ℹ️ No action; recorded for diagnostics |
| 2026-09-05 | Step 1 | `NODE_OPTIONS` carries a VS Code JS-debugger bootloader, which attaches a debugger to every `node`/`npm` invocation and pollutes stdout. | ℹ️ No action now; **must be cleared in CI** (Step 5) so it cannot corrupt scripted npm output |
| 2026-09-05 | Step 1 | Third-party accounts from `08-integrations.md` §7 are still unprovisioned. | ⏳ Not blocking until **Step 15** (Razorpay) / **Step 16** (logistics). **Updated 2026-09-06:** the logistics row now names **Shiprocket** (ADR-018) and needs an API user, each seller's pickup address registered and a webhook secret; the `manual` adapter carries fulfilment until it is complete |
| 2026-09-05 | Step 2 | Traefik mounts the Docker socket directly (read-only) to read container labels. `06-infrastructure-devops.md` §9 says the socket must never be mounted into an application container. | ℹ️ Acceptable in dev; **Step 32 must put a socket proxy in front of it** in staging/production |
| 2026-09-05 | Step 2 | `read_only: true` root filesystems (a §4 standard) are not applied to the data services — each needs its own tmpfs matrix. | ⏳ Deferred to **Step 32** with the rest of the production hardening |
| 2026-09-05 | Step 2 | The dev `data` network is not `internal: true` and publishes host ports (bound to `127.0.0.1`), which local tooling needs but §4 forbids in production. | ℹ️ Deliberate dev/prod difference, tabulated in `docs/dev-setup.md` §8; **Step 32 owns the production form** |
| 2026-09-05 | Step 2 | `docker compose --wait` returns as soon as *any* container exits, so the `minio-init` one-shot ended the wait early. | ✅ **RESOLVED** — the helper scripts start everything, then wait only on the five long-running services |
| 2026-09-05 | Step 2 | Non-browser clients (`psql`, `curl`, .NET `HttpClient`) cannot resolve `*.klarahome.localhost` on Windows; only browsers special-case `.localhost`. | ℹ️ Documented in `docs/dev-setup.md` §5 with a hosts-file snippet. **Revisit at Step 3**, when the API needs a public storage base URL |
| 2026-09-05 | Step 2 | MinIO is AGPLv3 and its licence terms have shifted repeatedly; the community image still ships the web console today. | ℹ️ ADR-010 already treats storage as an S3-API config swap, so the exposure is contained. No action |
| 2026-09-05 | Step 2 | No observability services (Prometheus/Loki/Grafana) in the dev stack. | ℹ️ Correct — `06-infrastructure-devops.md` §8 and the plan both place them at **Step 31**. Recorded so it is not mistaken for an omission |
| 2026-09-05 | Step 3 | **FluentAssertions 8.x is commercially licensed.** Three free options measured; the User chose to drop the assertion dependency entirely. | ✅ **RESOLVED** — xUnit built-in `Assert`, **ADR-012 accepted**, ADR-011 rejected and kept for the reasoning trail. `09-nfr-testing-observability.md` §2.1 updated. Nothing for Step 5 to guard |
| 2026-09-05 | Step 3 | `docker-compose.base.yml` still not created — the Step 2 note said the split would happen at Step 3, but the dev file is still the only compose file. | ⏳ Moved to **Step 32**, which creates the staging/production compose files the base file would serve |
| 2026-09-05 | Step 3 | `X-RateLimit-Limit/-Remaining/-Reset` are emitted on `429` responses only; `04-api-specification.md` §1 implies them on every response. The framework limiter does not expose the lease on the success path. | ⏳ Needs a custom limiter. Revisit at **Step 29** unless a client needs it sooner |
| 2026-09-05 | Step 3 | Output caching is in-memory; `01-architecture.md` §4.3 specifies Redis via `HybridCache`. | ⏳ Deferred to the caching work; contained to `OutputCacheExtensions` |
| 2026-09-05 | Step 3 | Handler/validator registration is startup assembly scanning, not the source-generated registration §4.3 anticipates. | ℹ️ One-off boot cost, nothing per request. Optimisation, not correctness |
| 2026-09-05 | Step 3 | ADR-001 – ADR-010 exist only as rows in `01-architecture.md` §9, not as files in `docs/adr/`, which §9 says they should be from Step 1 onward. | ℹ️ Documentation debt — the decisions are recorded and accepted. Backfill when one of them is next revisited |
| 2026-09-05 | Step 3 | The Worker and Migrator hosts are shells: configuration, logging and an exit-code contract, no work. There are no `worker`/`migrator` compose services or Dockerfiles yet. | ℹ️ Correct — their content is **Step 4** (outbox, migrations) onward. Recorded so the shells are not mistaken for finished work |
| 2026-09-05 | Step 3 | `.editorconfig` (a Step 1 artefact) was amended: accessibility modifiers no longer required on interface members, and CA1716 silenced. | ℹ️ Both changes justified in the Step 3 outcome notes. No specification impact |
| 2026-09-05 | Step 3 | `packages.lock.json` files are now committed (Step 1 set `RestorePackagesWithLockFile`). The Docker build restores **with a runtime identifier**, which produces a different lock file, so `dotnet restore --locked-mode` cannot be applied uniformly. | ⏳ **Step 5** must decide where `--locked-mode` is enforced — CI restore yes, image build no — or regenerate RID-specific lock files |
| 2026-09-05 | Step 3 | `dotnet test` now requires the Microsoft.Testing.Platform runner declared in `global.json`; the CLI form is `dotnet test --project <path>`. | ℹ️ **Step 5 must use this form.** A bare project path argument is rejected by the .NET 10 SDK |
| 2026-09-05 | Step 4 | **`dotnet test` reports `Zero tests ran` on this machine**, intermittently and then persistently, while the test executables run all 179 tests correctly. The Microsoft.Testing.Platform orchestrator reaches the host over a loopback JSON-RPC connection and reports zero rather than an error when that connection is refused — most likely an endpoint-security agent. | ⏳ **Step 5 must not depend on `dotnet test` alone.** Workaround documented in `docs/dev-setup.md` §7: run the test executable directly. CI should assert a **non-zero test count**, so a silent zero can never be mistaken for a pass |
| 2026-09-05 | Step 4 | `KlaraHome.Infrastructure` carries a `FrameworkReference` to `Microsoft.AspNetCore.App`, so the migrator — which serves no HTTP — must run on the `aspnet` base image rather than `runtime`. | ⏳ Splitting persistence/hosting out of Infrastructure would shrink the job image and sharpen the layering. Not urgent; revisit at **Step 29/31** unless another non-HTTP host appears first |
| 2026-09-05 | Step 4 | `pg_stat_statements` (required by `03-database-design.md` §1) is **not** installed: it needs `shared_preload_libraries`, a server setting rather than a migration. | ⏳ Belongs to the Postgres container configuration at **Step 31**, with the rest of the observability work |
| 2026-09-05 | Step 4 | Table partitioning from `03-database-design.md` §8 (`audit_logs`, `stock_ledger_entries`, `tracking_events`, `notification_messages`, `search_queries`) is not implemented. | 🔵 **`audit_logs` done at Step 6** — `PARTITION BY RANGE (occurred_at)`, monthly, hand-written SQL because EF cannot express it, with a `DEFAULT` partition as the safety net. The other four are still owned by the steps that create them |
| 2026-09-05 | Step 4 | Dapper, which `01-architecture.md` §4.3 pairs with EF for hot read paths, is not referenced. | ℹ️ No hot path exists. Add it when a specific query needs it, not before |
| 2026-09-05 | Step 4 | `platform.idempotency_keys` (`03-database-design.md` §4.1) is not created. | ⏳ It serves the `Idempotency-Key` header contract from `01-architecture.md` §6, which no step has built yet. Created by the step that enforces it — **Step 13/14** at the latest |
| 2026-09-05 | Step 4 | The outbox has no stored retry backoff: attempts are spaced by the poll interval and capped by `MaxAttempts`. A dead-lettered message stays in the table with `processed_at` NULL and needs a human. | ⏳ Adequate for v1. If dead letters become routine, revisit at **Step 31** with an alert rather than a schema change |
| 2026-09-05 | Step 4 | EF logs the "does the history table exist?" probe as an **Error** on a first migration against an empty database, which looks alarming in a deploy log although it is expected. | ℹ️ EF behaviour, first run only. Noted so it is not chased |
| 2026-09-05 | Step 4 | `Cannot load library libgssapi_krb5.so.2` is printed by Npgsql on every migrator run: the chiseled image has no krb5, so the Kerberos probe fails and authentication proceeds over SCRAM. The `-extra` chiseled variant does not carry krb5 either. | ℹ️ Harmless. Documented in `docs/dev-setup.md` §7 rather than paid for with a larger image |
| 2026-09-05 | Step 4 | `Tenant:Id` falls back to a value derived from `Tenant:Code` when unset. Stable across restarts, but changing the code would strand every row written under the old derived id. | ✅ **RESOLVED at Step 6, differently than planned.** Configuration stays the source of the tenant's identity — v1 is single-tenant-per-deployment and the objective is onboarding by configuration alone — so the risk is closed by detection rather than by moving the id into the database: the `platform-tenant` readiness check compares the configured tenant against `platform.tenants` and reports **Unhealthy** when the row is missing. A deployment that would serve an empty catalogue is now not ready instead of quietly wrong. Reasoning in the Step 6 card, deviation 1 |
| 2026-09-05 | Step 4 | Generated EF migrations are exempted from the `.editorconfig` style rules and from the "a module exposes nothing publicly" architecture rule, because `dotnet ef` emits them as `public partial` and offers no way to change that. | ℹ️ Narrow and compensated: a new architecture rule asserts the module `DbContext` itself is internal. No action |
| 2026-09-05 | Step 5 | **No GitHub remote exists** (`git remote -v` is empty), so the acceptance criterion "a pull request triggers the pipeline and blocks merge on failure" cannot be demonstrated. The workflow is `actionlint`-clean and every gate it calls was run locally. | ⛔ **Open — needs the User.** Create the remote and apply the ruleset in `docs/ci-pipeline.md` §6. Nothing else in the plan is blocked by it, but until then CI is a script rather than a gate |
| 2026-09-05 | Step 5 | `.editorconfig` amended again (a Step 1 artefact): private `const` and `static readonly` fields moved to PascalCase, and the `Async` suffix rule relaxed for `src/backend/tests/**`. 178 IDE1006 violations that `dotnet build` never reported. | ℹ️ Rule defects, not code defects; both justified in the Step 5 outcome notes. No specification impact |
| 2026-09-05 | Step 5 | `dotnet format` on Windows reports thousands of `ENDOFLINE` errors from stale CRLF files whose committed content is already LF. Fixed once by re-checking-out every `*.cs`. | ℹ️ Will recur on any fresh Windows clone that writes files before committing. Documented in `dev-setup.md` §7 with the one-line fix. CI is unaffected — a Linux checkout is LF |
| 2026-09-05 | Step 5 | 115 Nx-generated frontend files were reformatted by Prettier in one pass so `--check` could become a gate. | ℹ️ Scaffolding only, no logic. Lint, tests and both production builds re-verified afterwards |
| 2026-09-05 | Step 5 | `express` 4 stays, with `"overrides": { "qs": "^6.16.0" }` closing its three moderate advisories. Express 5 changes route-pattern syntax, and `app.use('/**', ...)` in the SSR server would have to be rewritten. | ⏳ **Step 23** owns the SSR server. Move to Express 5 there, and drop the override when it lands |
| 2026-09-05 | Step 5 | `@angular-devkit/build-angular` removed from `package.json`. It was an unused optional peer of `@nx/angular`; our apps use `@angular/build`. | ✅ **RESOLVED** — also closes the Step 1 note about the deprecated Webpack builder warning |
| 2026-09-05 | Step 5 | Coverage is gated on the **total** line rate, not per assembly. `SharedKernel` (62.2 %) and `Modules.Platform` (62.1 %) individually sit below 70 %, carried by `Api` at 98 % and `Infrastructure` at 91.6 %. | 🔵 **Unblocked at Step 6:** every assembly is now above the floor (`SharedKernel` 70.6 %, `Modules.Platform` 94.2 %, total 92.39 %). Turning the per-assembly gate on is still a **Step 29** decision, but nothing stands in its way now |
| 2026-09-05 | Step 5 | No SAST beyond the .NET analyzers and ESLint. `09-nfr-testing-observability.md` §2.1 lists SAST as continuous; CodeQL would be the obvious fit and is not in the Step 5 deliverables. | ⏳ Needs the User's decision. Cheap to add (one workflow, C# + TypeScript), but it is scope beyond the step card |
| 2026-09-05 | Step 5 | `src/frontend/.gitignore` still duplicates rules from the root file (raised at Step 1 as "consolidate at Step 5 if it causes confusion"). | ℹ️ It has caused none, and Nx regenerates it. Left alone deliberately |
| 2026-09-05 | Step 5 | Nx caching is local only; the CI jobs get no cross-run Nx cache, so lint, test and build re-run from scratch every time. | ℹ️ Frontend job runs in well under its timeout today. Nx Cloud or a self-hosted cache is a cost/latency decision, not a correctness one. Revisit if the job becomes slow |
| 2026-09-05 | Step 5 | The `containers` job builds images but pushes nothing, and there is no registry. | ℹ️ Correct — **Step 32** provisions the registry and adds push plus deploy. Recorded so the missing push is not read as an omission |

| 2026-09-05 | Step 6 | **`TenantOptions` was never bound in the worker or the migrator.** Both call `AddKlaraHomePersistence` but not `AddKlaraHomeInfrastructure`, so the ambient tenant fell back to its built-in defaults. Invisible only because the default code happens to be `klarahome`. | ✅ **RESOLVED** — binding moved into `AddKlaraHomePersistence`, where the ambient tenant is registered. Any host that writes a row now reads the tenant the same way |
| 2026-09-05 | Step 6 | **The connection string was captured at registration time** in `AddModuleDbContext`, so a host whose configuration completes after registration (`WebApplicationFactory`) registered every context against an empty string. | ✅ **RESOLVED** — resolved from the container when the context is built |
| 2026-09-05 | Step 6 | The audit-log partitions cover **24 months from the deploy date**. After that, rows land in the `DEFAULT` partition: everything keeps working but queries stop being pruned, and creating that month's partition then fails while its rows sit in the default. | ⏳ Scheduled partition maintenance belongs to **Step 31** with the rest of the operational jobs. `platform.ensure_audit_log_partition(date)` exists for it to call; the draining procedure is noted in the migration's own comment |
| 2026-09-05 | Step 6 | An audit entry is written in **its own transaction**, after the change it describes has committed. A crash in the window between them loses the entry. | ℹ️ Deliberate. The alternative — joining the caller's transaction — means an audit call can commit a caller's unsaved work, which is a far worse failure. Revisit only if a compliance review demands the stronger guarantee |
| 2026-09-05 | Step 6 | **The admin surface is unprotected.** Every endpoint under `/api/v1/admin` declares its permission, but no authentication scheme exists until Step 7. | ✅ **RESOLVED at Step 7.** `RequirePermission` now attaches the policy as well as the metadata, a dynamic provider manufactures every `perm:` policy, and a fallback policy closes anything that declares nothing. `UnsecuredEndpointGuard` is quiet because the Identity module registers the scheme; `AdminSurfaceTests` was rewritten to assert the enforcement rather than hold the gap open |
| 2026-09-05 | Step 6 | `FeatureRollout.Segments` cannot be used yet: nothing assigns a caller to a cohort until roles exist. | 🔵 **Half done at Step 7:** `ICallerContext.Segment` reads a `segment` claim and the evaluator handles it, but nothing yet defines what a cohort is or assigns one. Carried forward as its own Step 7 row |
| 2026-09-05 | Step 6 | `PagedResult.PrevCursor` is always null — the audit search pages forward only. | ℹ️ The UI keeps the cursors it has already seen. Backwards paging is a real feature, not a gap in this one; add it when a screen needs it |
| 2026-09-05 | Step 6 | `Microsoft.AspNetCore.OutputCaching` is **not on a module project's compile reference set** even with an explicit `FrameworkReference`, though `KlaraHome.Infrastructure` resolves it fine. | ℹ️ Worked around by wrapping `CacheOutput()` as `CacheReferenceData()` in Infrastructure, which is the better home for the policy name anyway. Recorded so the next module that reaches for an ASP.NET Core optional assembly knows what it will hit |
| 2026-09-05 | Step 6 | `platform.idempotency_keys` (`03-database-design.md` §4.1) is still not created, although the schema it belongs to now exists. | ⏳ Unchanged from Step 4: it serves the `Idempotency-Key` contract, and it is created by the step that enforces it — **Step 13/14** at the latest |
| 2026-09-05 | Step 6 | The `platform.pincodes` table ships **empty**; the India Post dataset is mounted from `infra/seed/`, not built into the image. | ⏳ Needs the client to supply the dataset before the address form can autofill. Documented in `infra/seed/README.md` and `docs/dev-setup.md` §6; the `platform.pincode-lookup` flag keeps the endpoint off meanwhile |
| 2026-09-05 | Step 6 | `hsn_codes` holds the 99 chapters with **no default GST rate**. | ⏳ Correct today — rates are notified at four to eight digits. **Step 12** (Pricing, Tax & Promotions) attaches real rates to real tariff items |
| 2026-09-05 | Step 6 | Reference tables (`states`, `pincodes`, `hsn_codes`) carry **no `tenant_id`**, a deliberate exception to the tenancy convention in `03-database-design.md` §1. | ℹ️ §1 governs *business* tables. These are identical in every deployment and nobody edits them; giving them a tenant would mean an address form that empties itself under a different tenant |
| 2026-09-05 | Step 6 | Settings and feature-flag caches are **in-process**, invalidated by the writer. | ⏳ Exact at one replica, eventually-consistent within the TTL at two. Folded into the existing Redis/`HybridCache` item from Step 3 |

| 2026-09-05 | Step 7 | **One-time codes and reset links are written to the API log.** `LoggingOtpDispatcher` is the only implementation of `IOtpDispatcher`, and logging an OTP is what `07-security-compliance.md` §3 forbids outright. | ✅ **CLOSED at Step 8.** `LoggingOtpDispatcher` is deleted. A code is rendered from an editable template, handed to a provider inline, and the row recording the attempt keeps neither the body nor any variable's value — verified against the database, not only asserted (ADR-017) |
| 2026-09-05 | Step 7 | **The breached-password check is a compiled-in list of ~60 entries, not the Have I Been Pwned range API.** | ⏳ Needs the SSRF allow-list from §3, a timeout policy and a decision about what to do when the API is down. Belongs to the step that builds the outbound HTTP policy; the offline list stays as the floor underneath it |
| 2026-09-05 | Step 7 | **Impersonation is not built.** `07-security-compliance.md` §2 requires time-boxed, reason-carrying, audited support impersonation. It is not in the Step 7 deliverables. | ⏳ Needs a support workflow to hang off. Revisit when the admin app has one — **Step 26/27** at the earliest |
| 2026-09-05 | Step 7 | **DPDP data export and erasure are not built.** `04-api-specification.md` §3.1 lists `POST /store/me/data-export` and `/delete-request`; §5 requires both. Not in the Step 7 deliverables, and erasure has to anonymise across schemas that do not exist yet. | ⏳ Cannot be finished before the modules holding the data exist. Belongs at **Step 29/31**, and is a launch-checklist item at Step 33 |
| 2026-09-05 | Step 7 | **`GET /store/me/notification-preferences` is not built**, though §3.1 lists it under the account surface. | ✅ **CLOSED at Step 8.** Built, with the categories a person may choose about and only the channels this deployment can actually deliver on |
| 2026-09-05 | Step 7 | **A vendor user is confined to exactly one seller.** The token carries a single `vendor_id` and the filter compares that one value, so a user with grants in two sellers would silently see only the lowest-numbered. | ℹ️ Deliberate. One person across two sellers means a second account, which is also what an access review expects to find. The resolver orders deterministically rather than taking whichever row came back first |
| 2026-09-05 | Step 7 | **Permissions are read from the access token, not the database, on every request.** A permission taken away therefore survives for up to one access-token lifetime unless something also ends the session. | ℹ️ Deliberate, and the reason the token is fifteen minutes. The paths that matter — a role change and a status change — revoke the sessions explicitly, so "revoked" means now for those |
| 2026-09-05 | Step 7 | **An unrouted path is answered 404 by a middleware placed before `UseAuthorization`.** ASP.NET Core applies the fallback policy to non-endpoint requests, which would otherwise make every unknown URL a 401. | ℹ️ Recorded because it is a non-obvious framework behaviour, and because anyone reordering the pipeline would reintroduce it. `ErrorContractTests` fails if it comes back |
| 2026-09-05 | Step 7 | **`Auth:Tokens:SigningKeys` is empty in Development, so the host mints an ephemeral RSA key per process.** Every access token dies with the container. | ℹ️ Warned about on every start, and refused outright outside Development. **Step 32** supplies the real key as a Docker secret |
| 2026-09-05 | Step 7 | The **compose dev stack ships a fixed `AUTH_ENCRYPTION_KEY`**, which is worthless as a secret because it is in `.env.example`. | ⏳ Fine for a dev database with no real enrolments. **Step 32** must supply a generated key per environment, and the `.env.example` comment says so |
| 2026-09-05 | Step 7 | **`identity.permissions` is a projection, and the seeder deletes rows whose codes leave the catalogue** — but the `role_permissions` rows referencing a retired code are left alone and become visibly stale. | ℹ️ Deliberate: a role losing a permission silently is worse than one showing an entry an operator can see and remove. Nothing reads either table to decide an authorisation |
| 2026-09-05 | Step 7 | **The refresh-token rotation chain is never pruned.** Every rotation leaves a spent row, so a long-lived session accumulates one per refresh. | ⏳ The rows are what makes reuse detection possible, so they cannot simply be deleted on rotation. A retention job — drop rows whose session ended more than N days ago — belongs to **Step 31** with the other operational jobs |
| 2026-09-05 | Step 7 | **`otp_challenges` is never pruned either**, and it is kept deliberately: the throttle counts recent rows, so deleting them resets the throttle. | ⏳ Same owner as the row above, **Step 31**. Neither table has an index that degrades before then |
| 2026-09-05 | Step 7 | **No authorisation matrix test (every endpoint × every role).** `07-security-compliance.md` §8 lists one as running on every CI run, and it is a Step 29 acceptance criterion. | ⏳ **Step 29** owns it. The declarations it would enumerate now exist and are asserted to be complete, which is the half that had to be true first |
| 2026-09-05 | Step 7 | **Rate-limit buckets partition per IP for auth, not per account.** `04-api-specification.md` §6 specifies "10/min per IP" for auth, which is what is implemented, but a distributed attempt against one account is only caught by the account lockout. | ℹ️ As specified. The lockout is the per-account control and it is progressive; recorded so the division of labour is not mistaken for a gap |
| 2026-09-05 | Step 7 | `AUTH_REFRESH_COOKIE_SECURE=false` in the dev compose file, because a browser will not return a `Secure` cookie over the plain-http dev stack. | ⏳ **Step 32** must ensure it is true in every deployed environment. A startup refusal like the signing key's would be the stronger guard; not added because the setting is legitimate under a proxy that terminates TLS elsewhere |
| 2026-09-05 | Step 7 | **`docker-compose.dev.yml` recreates `postgres` when the file changes**, and on this machine a native `postgresql-x64-18` service now owns port 5432, so the container cannot rebind. | ⛔ **Needs the User.** Stop the Windows service, or set `POSTGRES_PORT=5433` in `.env` (already documented in `dev-setup.md` §4 and §7). Step 7 was verified against a throwaway container on the same Docker network instead |
| 2026-09-05 | Step 7 | `.editorconfig`/`Directory.Build.props` amended again: **CA1861 silenced for test projects**. Hoisting a request payload's inline array to a static field is a hot-path allocation optimisation, and a test that posts one payload once is not a hot path. | ℹ️ Rule defect, not a code defect; production code keeps the rule. Same shape as the CA1707 exemption already there |
| 2026-09-05 | Step 7 | **`FeatureRollout.Segments` is still unusable**, though Step 6 expected this step to supply the value. `ICallerContext.Segment` reads a `segment` claim, but nothing assigns a cohort to a user. | ⏳ The plumbing is in place and the claim is read; what is missing is a definition of what a cohort *is*, which belongs to whichever step first needs one. Percentage and allow-list rollout both work today |
| 2026-09-05 | Step 7 | **`platform.idempotency_keys` is still not created.** Unchanged from Steps 4 and 6. | ⏳ Created by the step that enforces the `Idempotency-Key` contract — **Step 13/14** at the latest |

| 2026-09-05 | Step 7A | **The identity-provider row in `08-integrations.md` §7 is incomplete.** Google sign-in is built and tested, but this deployment owns no OAuth client and has published no privacy policy for the consent screen to link to. | ⛔ **Open — needs the User.** Under §7's own rule this is a `BLOCKED` condition, not a reason to stub. The code is finished and proven against a fake provider and against Google's real discovery document; what is missing is an account |
| 2026-09-05 | Step 7A | **The privacy policy and the processor register do not yet mention Google.** DPDP §5.6 requires a processor register, and a consent screen requires a published policy. | ⛔ **Needs the client.** A launch-checklist item at **Step 33**, and a prerequisite for the row above rather than a follow-up to it |
| 2026-09-05 | Step 7A | **Facebook cannot create an account with no email address.** `ck_users_has_identifier` requires a mobile number or an email, and a Facebook account may carry neither. | ⏳ Costs nothing while Facebook is disabled. The handler returns `IDENTITY_EXTERNAL_NO_EMAIL` naming exactly this, so whoever enables Facebook meets it immediately rather than debugging a constraint violation |
| 2026-09-05 | Step 7A | **The `id_token` signature is not re-validated.** The token arrives over TLS from the provider's own token endpoint in a direct server-to-server call, which OIDC §3.1.3.7 permits. | ℹ️ Correct as built, and recorded because it looks like an omission. It would stop being correct if the identity were ever accepted from the browser instead — which is exactly the flow ADR-014 decision 2 rejected |
| 2026-09-05 | Step 7A | **A customer whose provider email is unverified gets a second account**, or a conflict when the address is taken. | ℹ️ Deliberate (`07-security-compliance.md` §1). Linking on an unverified address is the standard account-takeover vector. It is a support cost, and the conflict message tells the person what to do instead |
| 2026-09-05 | Step 7A | **An administrator briefly knows a credential that would sign in as another person.** | ℹ️ ADR-014 decision 5, taken knowingly by the User with the trade-off stated. Narrowed by the forced change, the session revocation and the audit entry; closed when email delivery returns and the reset link makes the endpoint unnecessary |
| 2026-09-05 | Step 7A | **Google is an availability dependency.** A customer who registered that way cannot sign in while Google is unreachable; the endpoint answers `503`. | ℹ️ Email and password remain available to them, and `identity.external-login` can be turned off. Recorded because it is a new class of outage this system did not previously have |
| 2026-09-05 | Step 7A | **`FeatureGate` evaluates a flag on every gated request.** The evaluation is served from the Platform module's in-process cache, so it is not a query per request — but it is a cache lookup on the hot path of the auth endpoints. | ℹ️ Measured in nothing today. Folded into the existing Redis/`HybridCache` item if the settings cache ever moves |
| 2026-09-05 | Step 7A | **No provider-initiated logout, and no token revocation at the provider.** Signing out here ends our session and leaves the Google session alone. | ℹ️ Correct for a storefront — signing out of a shop should not sign somebody out of Gmail. Recorded so it is not mistaken for an omission |
| 2026-09-05 | Step 7A | **`external_logins` rows are never pruned**, including for accounts that are soft-deleted. | ⏳ Joins the refresh-token and OTP retention item at **Step 31**. A DPDP erasure will need to anonymise them too, which belongs to the same work |
| 2026-09-05 | Step 7A | **The Angular storefront does not exist yet**, so the redirect flow has been proved with `curl` and an integration test rather than a browser. The `SameSite=Lax` cookie behaviour in particular is asserted by its attributes, not by a real navigation. | ⏳ **Step 23** builds the storefront shell and is the first chance to drive this from a browser. Worth an explicit check there rather than assuming |
| 2026-09-05 | Step 7A | `AdminUserResponse` gained `mustChangePassword` and `passwordSetupPending`. Additive, so no client breaks, but the generated Angular client will regenerate at **Step 22**. | ℹ️ Recorded so the diff is expected rather than investigated |
| 2026-09-05 | Step 8 | **Nothing scans an upload.** `IVirusScanner` has one implementation, which records `scan_state = 'skipped'` and scans nothing. | ⏳ A ClamAV adapter is the intended second implementation and is not in Step 8's deliverables. `Media:RequireVirusScan` turns the gap into a refusal for a deployment that cannot accept it; the default is off, because a deployment that refused every upload would be useless |
| 2026-09-05 | Step 8 | **The SMS and WhatsApp rows of `08-integrations.md` §7 are still incomplete**, so both channels are suppressed in production. | ⛔ **Open — needs the client.** Everything above the adapter is built and exercised: templates, DLT registry, queue, retry, preferences, delivery log. The day an account exists it is one adapter and one setting |
| 2026-09-05 | Step 8 | **The seeded SMS templates carry no DLT template id**, so they are seeded inactive in Production. | ⛔ Same owner as the row above. They are shape-validated, and a test asserts each would satisfy the length rules the moment a registration exists |
| 2026-09-05 | Step 8 | **A failed one-time code is not retried.** Sensitive messages are sent inline and their bodies are not stored, so there is nothing to re-send. | ℹ️ Deliberate. By the time a retry ran the person would have pressed "resend" and been issued a different code, and holding the plaintext to retry with is what this design refuses to do. The admin retry endpoint refuses these explicitly rather than failing oddly |
| 2026-09-05 | Step 8 | **`notification_messages` and its partitions are never pruned**, and §8 requires message bodies to be retained for ninety days rather than for ever. | ⏳ **Step 31**, with the audit-log partition maintenance and the refresh-token and OTP retention already parked there. Two years of monthly partitions plus a default partition exist, so nothing fails before then |
| 2026-09-05 | Step 8 | **`media.files` rows are never pruned, and nothing counts references.** A file deleted while a product still points at it leaves a dangling id; a file nobody points at is never collected. | ⏳ Deliberate for now — deletion is soft so a stored id resolves to nothing rather than to an error (ADR-016). An orphan sweep needs the modules that hold the references to exist, so it belongs at **Step 31** |
| 2026-09-05 | Step 8 | **imgproxy URLs are unsigned in the development stack**, because `IMGPROXY_KEY` and `IMGPROXY_SALT` are blank. | ⏳ Fine on a loopback, a defect anywhere else: unsigned, imgproxy resizes for anybody who finds it, on this deployment's bandwidth and domain. **Step 32** must generate a pair per environment; `.env.example` and `dev-setup.md` §3 both say so |
| 2026-09-05 | Step 8 | **A presigned URL is signed for a different host from the one the API writes through**, which needed a second S3 client. | ℹ️ Recorded because it looks like duplication and is not: SigV4 covers the host, so a URL signed for the internal address cannot simply be rewritten. AWS S3 and R2 need no second client, because their addresses already agree |
| 2026-09-05 | Step 8 | **The configuration binder *appends* to a collection-typed property that already has a value.** A default of four rendition widths plus the same four in `appsettings.json` produced eight, and a response reading "thumb, small, thumb, small". | ℹ️ Found by an integration test, fixed by normalising in the builder rather than by trusting the binder. Worth knowing before the next `IReadOnlyList<T>` setting is added |
| 2026-09-05 | Step 8 | **A blank `Auth:Tokens:SigningKeys:0:PrivateKeyPem` was parsed as a malformed key**, so a Development stack answered every request with "No supported key formats were found" instead of minting the ephemeral key the compose file documents. | ℹ️ **Fixed in passing**, one line: a blank entry is now treated as absent. Out of Step 8's scope, but it blocked the step's own demonstration and the behaviour was already documented as working |
| 2026-09-05 | Step 8 | **The admin surface test matched route prefixes with `StartsWith`**, so `/admin/media` counted as part of the `/admin/me` self-service surface and was required to declare no permission. | ℹ️ Fixed to match on whole path segments. The test was right about the rule and wrong about which endpoints it covered — the failure mode of a prefix check nobody had a collision for yet |
| 2026-09-05 | Step 8 | **`ChannelTest` messages accumulate in the delivery log**, one per press of "send test". | ℹ️ Harmless and deliberate: a test message is a real message, and hiding it would make the log a partial record. Folded into the Step 31 retention work |
| 2026-09-05 | Step 8 | **The delivery log has no metrics or alert.** "How many messages are we failing to send" is a query an operator has to run. | ⏳ **Step 31** owns observability. The counters are cheap once there is somewhere to send them, and the suppression reason is already the dimension worth grouping by |
| 2026-09-05 | Step 8 | **A rendition wider than the original is not offered**, so a small logo has fewer variants than a photograph. | ℹ️ Deliberate: upscaling costs bandwidth to deliver a blurrier picture. Recorded because a storefront that assumes four renditions will be surprised by two — **Step 24** should read the list rather than assume it |
| 2026-09-05 | Step 8 | **`Documents__FontDirectories` decides what an invoice looks like**, and the container image supplies the font at build time rather than the repository carrying one. | ℹ️ A statutory document that renders differently per host is the failure this avoids; the renderer refuses to start rather than emitting empty boxes. Worth re-checking at **Step 32** if the base image changes |
| 2026-09-05 | Step 8 | **The worker has no health check and publishes no port**, so "is it working" is answered by the queues draining rather than by a probe. | ⏳ Correct for a background host, and it means a stuck worker is invisible until a backlog builds. **Step 31** adds the backlog-age alert that makes it visible |
| 2026-09-05 | Step 9 | **`IOutbox` was bound to whichever module registered first**, so any module but Platform publishing through it would have written the row to a different context's change tracker and lost the event with no error anywhere. Found while wiring `VendorActivated`, the first integration event this platform raises. | ✅ **FIXED in the shared layer**, minimally: `AddModuleDbContext` now also registers a **keyed** `IOutbox` per context, and a module asks for its own by name (`[FromKeyedServices(typeof(VendorsDbContext))]`). The unkeyed registration is left for the first-registered context. **Every future module that publishes must use the keyed one** — worth an architecture test at Step 29 |
| 2026-09-05 | Step 9 | **Column encryption was promoted to the shared layer** as `IFieldProtector` + the `Encryption` configuration section, because a seller's bank account must be readable again and so cannot be hashed. The Identity module still has its own AES-GCM envelope under `Auth:Encryption` for the TOTP secret. | ⏳ **Two key rings now exist with the same shape, and the dev environment gives them the same key material.** Fold `Auth:Encryption` into the shared one at **Step 29/31**; doing it now would have meant editing Step 7 code mid-sprint |
| 2026-09-05 | Step 9 | **`string.Normalize(FormD)` does not decompose under `InvariantGlobalization`**, which this product sets. A normalise-and-strip-marks slug turned "Café" into `caf-` rather than `cafe`. | ℹ️ **Fixed with an explicit fold table** in `VendorFormats.ToSlug`, and a test. Worth knowing before the next place that folds diacritics — the catalogue slugs at **Step 10** are the same problem |
| 2026-09-05 | Step 9 | **`vendor_serviceable_regions` did not exist in `03-database-design.md` §4.3**, but "serviceable regions" is a Step 9 deliverable. | ✅ **Specification updated first** (protocol rule 9) and logged in `CHANGE_LOG.md`. §4.3 also now records the columns this step actually built |
| 2026-09-05 | Step 9 | **The Vendors permission constants are declared twice** — in `VendorPermissions` and in Identity's `PermissionCatalog` — because a module may not reference another module. | ℹ️ The same arrangement Media already uses. The test that asserts the two lists agree is in `TEST_DEBT.md`; until Step 29 runs it, a typo in one would be a role checkbox that grants nothing |
| 2026-09-05 | Step 9 | **Nothing checks a bank account.** `verify` records a human's decision; there is no penny-drop integration. | ⏳ Correct for v1 — a penny drop needs the Razorpay credentials that **Step 15/18** own. The seam is the endpoint, and the field it writes is already what activation reads |
| 2026-09-05 | Step 9 | **No Razorpay Route linked account is created.** `IVendorPayoutAccounts` has one implementation that creates nothing and logs which seller would have been provisioned. | ⏳ By design, and named in the step card as Step 18's. Activation deliberately does not fail when provisioning does: the seller trades and cannot yet be paid, which Settlements has to handle regardless |
| 2026-09-05 | Step 9 | **A seller's staff must already exist as an Identity account**, and the two calls are not one transaction: create the user, grant the vendor role, then add the membership. A crash between them leaves a user with a vendor role and no membership row. | ⏳ Tolerable — the membership is a directory, not the authority; the `vendor_id` claim comes from the role grant. **Step 28's vendor portal** should do the two calls behind one screen, and Step 29 should decide whether an orphan sweep is worth it |
| 2026-09-05 | Step 9 | **Commission resolution matches a category exactly**, so a rule on Furniture does not catch a sale of Sofas. The caller passes whichever ancestor it wants matched. | ⏳ The category tree is Catalog's, and it does not exist yet. **Step 18** is the first caller that has to decide how far up the tree to walk; the contract already takes the id it wants matched |
| 2026-09-05 | Step 9 | **`vendors.rating` is written by nobody.** The column and the API field exist; the aggregate that fills them is Reviews. | ℹ️ Correct — **Step 21** owns review read-models. Recorded so a null rating is not mistaken for a defect |
| 2026-09-05 | Step 9 | **`vendor_serviceable_regions` is stored and never evaluated.** Nothing yet asks "does this seller deliver to 560001". | ⏳ The rules are the seller's input; the evaluation belongs to serviceability at **Step 16**, alongside the aggregator's own answer. The allow-list-with-exclusions shape is what that step will read |
| 2026-09-05 | Step 9 | **A suspended seller's listings are not taken down by anything.** `VendorSuspended` is published and has no consumer. | ⏳ Catalog (**Step 10**) and Search (**Step 19**) are the intended consumers. The outbox dispatcher logs and marks an event with no handler as processed, so nothing accumulates in the meantime |
| 2026-09-05 | Step 9 | **KYC documents are never pruned**, and a rejected scan of a PAN card stays in the private bucket indefinitely. | ⏳ Joins the retention work already parked at **Step 31**. A DPDP erasure will need to reach these too |
| 2026-09-05 | Step 9 | **`GET /admin/vendors/me` is the only "mine" route.** Every other seller-facing route takes the seller's id in the path, and a vendor caller must pass their own. | ℹ️ Deliberate: one set of URLs for both audiences, scoped by the token. The vendor portal at **Step 28** reads `me` once and uses the id from then on |
| 2026-09-05 | Step 10 | **XLSX bulk import/export is not built.** The deliverable says "CSV/XLSX"; only CSV exists, through a hand-written ninety-line RFC 4180 reader and writer with no dependency. Every mature .NET XLSX library is either under a licence `Directory.Packages.props` forbids or carries a transitive dependency tree whose licences could not be verified offline. | ⛔ **Open — needs the User.** Two routes: add `DocumentFormat.OpenXml` (MIT, Microsoft) and write the SpreadsheetML mapping, or declare CSV sufficient and amend the deliverable. The reader is already behind a seam — the job carries a file name and a content type — so a second format is one class and one branch |
| 2026-09-05 | Step 10 | **`GET /store/products`, the faceted product listing, is not in this module.** It reads a denormalised projection with facet counts, which is the `search` schema. | ℹ️ Correct: **Step 19** owns it. §3.2 now says so. Recorded so its absence is not mistaken for an omission — the storefront at Step 24 must read it from Search, not from Catalog |
| 2026-09-05 | Step 10 | **Product descriptions are stored exactly as given.** The field is documented as "HTML, sanitised on the way in" and nothing sanitises it. Every writer holds `catalog.product.manage`, so this is not an anonymous injection path, but a vendor user is not trusted with script on a PDP. | ⏳ **Step 29**, with the rest of the security work. It is one sanitiser at the write boundary, and the same one CMS (Step 20) will need — which is why it belongs in the shared layer rather than here |
| 2026-09-05 | Step 10 | **The buy-box index has no `INCLUDE` column**, which `03-database-design.md` §5 asks for. EF's `IncludeProperties` takes a scalar property name and the selling price is a complex property; neither `"SellingPrice.Amount"` nor `"selling_price_amount"` resolves. | ⏳ **Step 29** — a one-line raw-SQL migration step, once a query plan says the heap fetch costs something. The index itself exists and is correct; only the covering optimisation is missing |
| 2026-09-05 | Step 10 | **`VendorSummary` gained a `Rating` field**, and `VendorDirectory` now projects `vendors.rating` into it, because the buy box ranks partly on the seller's rating and may not read the sellers' table. | ℹ️ A one-field contract addition with one call site. Worth knowing that **`vendors.rating` is still written by nobody** (Step 21), so today every seller is unrated and the criterion falls through to `TreatUnratedAsAverage` |
| 2026-09-05 | Step 10 | **Reactivating a seller does not put their offers back.** `VendorSuspended` withdraws every live listing; `VendorActivated` deliberately does nothing but log. | ℹ️ Deliberate. Whether an offer was paused by its seller or by the platform is not recorded on the listing, and republishing a price a suspended seller never reviewed is worse than making them press the button. **Step 28's vendor portal** should offer a "republish all" action |
| 2026-09-05 | Step 10 | **The category tree is loaded whole and assembled in memory.** One query, then recursion over a few hundred rows. | ℹ️ Correct at this size and wrong at ten thousand nodes. Recorded because the failure mode is a slow storefront rather than an error; **Step 29** should measure it before anybody optimises it |
| 2026-09-05 | Step 10 | **Nothing prunes finished `catalog_jobs` rows or the CSV objects behind them.** An import file and its export sit in the private bucket indefinitely. | ⏳ Joins the retention work already parked at **Step 31**, alongside notification messages, audit partitions and KYC documents |
| 2026-09-05 | Step 10 | **An import writes no audit trail per row.** The job is audited when it is queued; the several hundred products it then creates or edits are not individually in `platform.audit_logs`. | ⏳ Deliberate for now — a thousand-row import would be a thousand audit rows, and the job's own report already says exactly which SKUs changed. **Step 29** should decide whether the audit trail needs them, since a compliance auditor may not accept the job report as the record |
| 2026-09-05 | Step 10 | **Commission resolution still matches a category exactly** (the Step 9 note). The category tree now exists, and `categories.path` makes "walk up to the nearest ancestor with a rule" a single prefix query. | ℹ️ Unblocked, not done. **Step 18** is the first caller with a reason to decide; the materialised path is what it will need and it is now there |
| 2026-09-05 | Step 10 | **A product's slug is not versioned and no redirect is written when it changes.** Renaming a published product silently breaks every existing link to it. | ⏳ `content.redirects` exists in `03` §4.14 and is **Step 20's** table. Recorded so the two are joined up there rather than discovered by a support ticket |
| 2026-09-05 | Step 10 | **The importer resolves categories and brands by slug, and creates neither.** A row naming a category that does not exist is rejected rather than creating it. | ℹ️ Deliberate: a typo in a spreadsheet must not silently add a category to the browse tree, and the taxonomy is platform-owned while an import may be a seller's. The error names the slug it could not find |
| 2026-09-05 | Step 10 | **The `catalog-manager` and vendor role bundles were filled in** — they have been empty or near-empty since Step 7, waiting for endpoints to grant. System roles reassert their permission set on every deploy. | ℹ️ Expected, and the mechanism Step 7 documented. Worth knowing that a deployment that had hand-edited those roles will have the edits undone on the next deploy |
| 2026-09-05 | Step 10 | **`OutputCacheExtensions.CachePublicRead` was added to the shared layer**, the short-TTL sibling that file's own comment said would arrive with "the first catalogue endpoint that needs it". Output caching is still in-memory rather than Redis-backed (a Step 3 note). | ℹ️ Two lines, no behaviour change elsewhere. The Redis question is unchanged and still parked |
| 2026-09-06 | Step 11 | **The stock ledger gained two columns `03-database-design.md` §4.5 did not have** — `reserved_change` and `reserved_after`. §4.5 says *both* quantity columns are caches reconciled against the ledger sum, and without a signed reserved delta that claim is unverifiable for half of them. | ✅ **Specification updated first** (protocol rule 9) and logged in `CHANGE_LOG.md`. Also amended: the batch/lot and serial tables a named deliverable needs, and columns for the seven tables §4.5 named but never described |
| 2026-09-06 | Step 11 | **`StockRunningLow` is published and nothing consumes it.** The low-stock alert reaches the outbox and stops there; there is no Notifications template and no handler. | ⏳ **Step 21 or Step 29** — one template plus one handler. The outbox dispatcher marks an event with no handler as processed, so nothing accumulates in the meantime. Until then the deliverable "low-stock thresholds and **alerts**" is half met: the threshold is detected and nobody is told |
| 2026-09-06 | Step 11 | **A transfer has no header table.** The two legs share a `reference_type = 'transfer'` and a generated reference id, and §4.5's reason list already carries `transfer_in`/`transfer_out`. | ℹ️ Deliberate — a header table would add a document nobody asked for. **Step 16** may want one if a transfer becomes a shipment with a courier and a tracking number; the reference id is already the join key it would need |
| 2026-09-06 | Step 11 | **Serial tracking is capture-only.** Units are booked in and can be moved between states, but nothing moves them automatically — a sale does not mark a serial `Sold`, and `HoldAsync` does not allocate specific units. | ⏳ **Step 14 (Orders)** is the first caller that knows which units left. The states and the `reference_id` column are already there; what is missing is the code that writes them |
| 2026-09-06 | Step 11 | **Batch quantities are not enforced against on hand.** `stock_batches.quantity` is a breakdown of the row's on-hand total and nothing asserts they add up. | ℹ️ Deliberate: on hand is the ledger sum and nothing else. Making it depend on batches would put a second source of truth beside the first. **Step 29** should extend the reconciliation query to report a batch/on-hand mismatch as a second, separate drift |
| 2026-09-06 | Step 11 | **Nothing sweeps expired batches.** `stock_batches.expires_on` is indexed and read by nobody; expired stock stays sellable. | ⏳ Correct for a homeware catalogue today and wrong the day a category with a shelf life is added. Joins the scheduled-jobs work at **Step 31**; the index it needs already exists |
| 2026-09-06 | Step 11 | **`HoldAsync` never splits a line across warehouses.** It walks the seller's active locations in priority order and takes the first that can supply the whole quantity; if none can, the hold is refused even when the units exist across two shelves. | ⏳ **Step 16** owns the decision, because a split line is two parcels, two dispatch SLAs and two shipping charges. Recorded because the refusal looks like an oversell guard misfiring and is not |
| 2026-09-06 | Step 11 | **The Inventory permission constants are declared twice** — in `InventoryPermissions` and in Identity's `PermissionCatalog` — because a module may not reference another module. | ℹ️ The fourth module to do this, after Media, Vendors and Catalog. Unlike the others, a **unit test now asserts the five codes are in the catalogue** (`StockReservationTests`), but from a hand-written list rather than by reflecting over the endpoints. The reflective form is in `TEST_DEBT.md` |
| 2026-09-06 | Step 11 | **`StockLedgerService` runs raw ADO commands rather than EF `ExecuteUpdate`.** EF cannot express `RETURNING` on an update, and the returned balances are what the ledger entry is made of. | ℹ️ Deliberate and confined to one file. Two things it has to do by hand because EF is not driving: enlist in the ambient transaction, and reset the entity's `xmin` from `RETURNING`. Both are asserted only by inspection today — see `TEST_DEBT.md` |
| 2026-09-06 | Step 11 | **A movement flushes an `Added` stock item on the caller's behalf.** A row that has not been inserted cannot be updated, so `ApplyAsync` saves when the entity is in `Added` state. | ℹ️ Cheaper than making every caller remember, and the only case where this service saves. Worth knowing it exists, because it means a movement on a brand-new row is not perfectly atomic with the insert that created it |
| 2026-09-06 | Step 11 | **The reconciliation job reports and does not repair**, and it reports to the log rather than anywhere an operator will see. | ⏳ Reporting is right; the destination is not. **Step 31** owns alerting, and a drift row is exactly the kind of thing that should page somebody. Recorded so "it is logged" is not mistaken for "somebody will notice" |
| 2026-09-06 | Step 11 | **Partition maintenance for `inventory.stock_ledger_entries` is not scheduled.** Twenty-five months exist from first migration; after that, inserts land in the default partition and stay correct but unpartitioned. | ⏳ **Step 31**, with `platform.audit_logs` and `notifications.notification_messages`. Note the difference: ledger entries have **no retention limit**, so the job must create partitions and never drop them |
| 2026-09-06 | Step 11 | **A stock write-off ignores existing holds.** `MoveAsync` guards only that on hand stays non-negative, so writing off damaged goods somebody has in their basket can leave `quantity_reserved > quantity_on_hand`. | ℹ️ Deliberate — the units really are damaged, and refusing the write-off would make the books wrong instead of the shelf. The check constraint permits it only where a backorder or pre-order flag is set, so an ordinary row would refuse the write; **Step 29 must decide** whether that refusal or a shortfall report is the right behaviour |
| 2026-09-06 | Step 11 | **`ListingPublished` opens a stock row in whichever active location has the lowest priority number**, preferring the seller's own over the platform's. A seller with no active warehouse gets no stock row and a log line. | ℹ️ Deliberate — inventing a location would be worse. **Step 28's vendor portal** should make "add your first warehouse" part of onboarding, since without one a seller's live listings are untracked and unsellable |
| 2026-09-06 | Step 11 | **The Operations role bundle gained the whole Inventory surface**, and both vendor bundles gained parts of it. System roles reassert their permission set on every deploy. | ℹ️ Expected, and the mechanism Step 7 documented. `vendor-staff` deliberately does **not** get `inventory.warehouse.manage` or `inventory.purchasing.manage` — opening a location and committing the seller's money are the owner's decisions |
| 2026-09-06 | Step 12 | **`ListingSummary` gained `CategoryId`, `CategoryPath` and `BrandId`.** Promotions are scoped to categories and brands, and a promotion on a parent category must reach everything beneath it - neither is answerable from the contract as it stood, and Pricing may not read `catalog.categories`. | ℹ️ A three-field contract addition with one call site, and the `ProductCatalogDirectory` join goes from three tables to four. The **path** rather than a parent id is what matters: it turns "is this line under Furniture" into a substring test instead of a tree walk on every cart line. Same shape as the Step 10 `VendorSummary.Rating` addition |
| 2026-09-06 | Step 12 | **A `pricing` store-settings section was added** to `StoreSettingsSections.cs` and registered in `SettingsCatalog`: the COD handling fee, the tax rate on delivery, whether the total rounds to a rupee, the loyalty accrual rate and the store-credit ceiling. | ℹ️ The same split every module here draws - a shopkeeper's decisions where a shopkeeper can change them, the platform's limits in `appsettings`. Not public: a shopper sees the COD fee on their quote, never the rule behind it, and publishing the wallet ceiling would publish the shape of an abuse |
| 2026-09-06 | Step 12 | **`QuoteRequest.IsFirstOrder` is supplied by the caller, not looked up.** Pricing cannot ask whether a shopper has ordered before without querying the Orders schema on the hottest path in the basket. | ⚠️ Deliberate, and it is a **hand-off Step 13 and Step 14 must honour**. It defaults to `false`, so a first-order campaign silently fails to apply until Cart and Checkout pass the flag. The safe direction is the one chosen: an offer that does not apply is a support question, one that applies to a returning customer is money |
| 2026-09-06 | Step 12 | **The store's own free-shipping threshold is applied by the quote engine**, on top of any free-shipping promotion, measured against the discounted total. | ℹ️ `CommerceSettings.FreeShippingThreshold` has existed since Step 6 and nothing read it. Measuring it after discount is deliberate - a shopper who used a coupon to reach the threshold has not spent it. **Step 16** owns the shipping rate itself and should check the two do not disagree |
| 2026-09-06 | Step 12 | **Nothing consumes `Pricing.PriceChanged` or `Pricing.PromotionRedeemed`.** Both reach the outbox and stop there. | ⏳ Expected: `PriceChanged` is **Step 19's** (the Search projection) and **Step 21's** (price-drop alerts); `PromotionRedeemed` is Step 21's reporting. The outbox dispatcher marks an event with no handler as processed, so nothing accumulates. The same shape as `StockRunningLow` from Step 11 |
| 2026-09-06 | Step 12 | **A price list is switched on or off without knowing the previous price**, so `PriceChanged` carries `PreviousPrice = null` for that case. | ℹ️ Honest rather than convenient: which price an offer had before depended on which *other* list was winning per offer, and guessing would be worse than saying nothing. A consumer that needs the delta re-reads `IPriceCatalog`. **Step 19** should confirm that is enough for its projection |
| 2026-09-06 | Step 12 | **Nothing sweeps expired store credit.** `wallet_transactions.expires_at` is written and read by nobody; credit issued with an expiry stays spendable. | ⏳ The `Expiry` transaction type exists for the sweeper that does not. Joins the scheduled-jobs work at **Step 31**. Low urgency while `pricing.store-credit` ships off |
| 2026-09-06 | Step 12 | **Loyalty accrual is designed and not wired.** `PricingSettings.LoyaltyAccrualPercent` and `LoyaltyMaxAccrualPerOrder` exist and `IStoreCredit.CreditAsync` takes the accrual reason, but nothing computes an accrual. | ⏳ Deliberate - the trigger is an order completing, and that is **Step 14's** event. The card asked for the wallet to be designed now and enabled by a flag, which is what was built |
| 2026-09-06 | Step 12 | **`PromotionLedger` runs `ExecuteUpdateAsync` alongside a tracked `SaveChangesAsync`** inside an explicit transaction. | ℹ️ The conditional increment cannot be expressed through the change tracker, and the claim, the redemption row and the event have to commit together. Confined to one file. Asserted only by inspection today - see `TEST_DEBT.md` |
| 2026-09-06 | Step 12 | **A quote and a redemption can disagree.** `RedeemAsync` returns the promotions it could not claim, because somebody may have taken the last use between quoting and placing. | ⚠️ **Step 14 must handle that return value.** The contract states it, and the refusal list is not an exception - an order that ignores it charges a discount the platform did not grant |
| 2026-09-06 | Step 12 | **The wallet's idempotency is a check-then-act guarded by a unique index**, not a single atomic statement. | ℹ️ The index is the real guarantee; the read makes the ordinary replay cheap. A genuine race produces a unique-violation exception rather than a clean no-op, which **Step 29** should turn into a caught conflict. Until the store-credit flag is on, no path reaches it |
| 2026-09-06 | Step 12 | **`PricingSettings.WalletMaxRedeemPercent` is enforced in the quote and nowhere else.** A caller that debits credit directly through `IStoreCredit.RedeemAsync` is not held to the ceiling. | ⚠️ **Step 14** is the only intended caller and it redeems what the quote said. Recorded because the ceiling reads like an invariant and is a quoting rule |
| 2026-09-06 | Step 12 | **The Pricing permission constants are declared twice** - in `PricingPermissions` and in Identity's `PermissionCatalog` - because a module may not reference another module. | ℹ️ The fifth module to do this, after Media, Vendors, Catalog and Inventory. The reflective test that would catch a drift is an integration test and is in `TEST_DEBT.md`; the codes were diffed by hand at this boundary |
| 2026-09-06 | Step 12 | **The `merchandiser` role bundle was filled in** - empty since Step 7 - and `finance`, `support` and both vendor bundles gained pricing permissions. System roles reassert their permission set on every deploy. | ℹ️ Expected, and the mechanism Step 7 documented. `merchandiser` deliberately gets **no** `pricing.tax-rate.manage`: what a supply is taxed at is the law's answer, not a merchandising decision. A seller gets price lists and no promotions - a marketplace-wide coupon is the platform's |
| 2026-09-06 | Step 12 | **`POST /store/quote` is anonymous and unauthenticated**, and it reads the catalogue, the price lists and every live promotion. | ℹ️ Capped at `Pricing:MaxQuoteLines` (100) and `MaxPromotionCandidates` (100) and rate-limited on the storefront-read policy, because without a ceiling it is a denial-of-service primitive with a friendly name. **Step 29** should load-test it - it is the most expensive anonymous read this platform has |
| 2026-09-06 | Step 12 | **Order-level discounts are allocated pro rata across matching lines, with the residue on the largest line.** The alternative - rounding each share independently - loses or gains paise, and the loss lands on the tax figures. | ℹ️ Correct and unit-tested. Worth knowing that a line's allocated share is therefore **not** independently reproducible from that line alone; it depends on the whole basket, which is why Orders must snapshot it rather than recompute it |
| 2026-09-06 | Step 12 | **Promotion `scope` and `conditions` are `jsonb` and nothing validates their contents at the database.** A bundle with no members or a tier ladder with no steps is storable. | ℹ️ The API validators reject both, and the engine treats either as "does not apply" rather than failing. Recorded because a row written by a future importer or by hand would bypass the validator. **Step 29** should decide whether a check constraint is worth the rigidity |
| 2026-09-06 | Step 12 | **`GET /admin/prices/resolve` and `GET /admin/tax-rates/resolve` exist to explain an answer**, and neither is in the original `04-api-specification.md` §4 list. | ℹ️ Added to §4 in the same amendment. They are the routes that answer "why was this priced at that" without an operator reading SQL, and they call the same resolvers the engine does, so their answer is the answer |
| 2026-09-06 | Step 13 | **`ICustomerDirectory` added to `KlaraHome.Contracts`** and implemented by the Identity module. A checkout has an address id and needs an address, and no foreign key may cross a schema. Every address read is keyed on **(customer, address)**, so an id belonging to somebody else does not resolve | ℹ️ Shared-layer addition, unavoidable and the mirror of `IVendorDirectory`. Orders (14), Payments (15) and Notifications will all read it |
| 2026-09-06 | Step 13 | **`IVendorDirectory` gained `IsServiceableAsync`.** "Serviceable regions" is a named Step 13 deliverable and the contract had no way to ask. Implemented in `VendorDirectory`; the rule set is read once and matched in memory, and an exclusion wins over an inclusion wherever both match | ℹ️ Shared-layer change. **Step 16 must not build a second serviceability answer** — the courier's reach is a different question from the seller's willingness |
| 2026-09-06 | Step 13 | **`IShippingOptions` declared at Step 13 and filled by a degenerate implementation.** `StandardShippingOptions` offers one free "Standard delivery" at the seller's own dispatch SLA with a flat 3-7 day window | ⏳ **Step 16 replaces it.** Registered with `TryAdd`. `CommerceSettings.FreeShippingThreshold` is *not* applied by it — shipping is zero anyway — and Step 16 must honour it |
| 2026-09-06 | Step 13 | **`IOrderPlacement` declared at Step 13 with a refusing implementation.** `UnavailableOrderPlacement` returns `503 ORDERING_UNAVAILABLE`, so **`POST /store/checkout/{id}/place-order` cannot succeed on this build** | ⏳ **Step 14 replaces it.** Deliberately a refusal rather than a stub that invents an order number: an order that exists in a response and nowhere else is worse than no order |
| 2026-09-06 | Step 13 | **The stock holds and the cart's own transaction are not one atomic unit.** The hold is written by Inventory's context, the conversion by this module's. Every failure path after the hold calls the compensating release explicitly, and Inventory's sweeper is the backstop | ⏳ Accepted for the sprint. **Step 29 must prove every release path runs**; a distributed transaction is not the answer, but the compensations need testing |
| 2026-09-06 | Step 13 | **Idempotency-key handling is module-local**, in `carts.checkout_placements`, rather than shared infrastructure. Payments (Step 15) and any retryable admin write will want the same thing | ⏳ **Step 15 must not build a second one.** Promote it to `KlaraHome.Infrastructure` when the second caller appears, not before |
| 2026-09-06 | Step 13 | **`CartAbandoned` has no consumer**, so the reminder half of the abandoned-cart deliverable is not delivered. `Cart.RecordReminder` and `reminder_count` exist and are never written to | ⏳ Belongs to **Step 20/21** (campaigns) or to a Notifications trigger. The capture is complete; the outreach is not |
| 2026-09-06 | Step 13 | **Store credit cannot be applied at checkout.** `CartRenderContext.WalletRedeemRequested` is plumbed through to the quote engine but no endpoint sets it | ⏳ Needs a `PUT /store/checkout/{id}/wallet` and the `pricing.store-credit` flag. Raise at the Step 14 boundary |
| 2026-09-06 | Step 13 | **`QuoteRequest.IsFirstOrder` is always false**, so Pricing's first-order campaigns cannot fire from a cart or a checkout. Only Orders can answer "is this their first" | ⏳ **Step 14 must supply it**, exactly as `IPriceQuoteEngine` documents. Until then a first-order offer silently does nothing |
| 2026-09-06 | Step 13 | **The merge clamp reads only `CommerceSettings.MaxQuantityPerLine`**, not the seller's own `MaxOrderQuantity`. Add and update both honour the tighter of the two; the merge does not, because it has no catalogue read | ℹ️ A merged line can briefly exceed a seller's per-order cap. The next render prices it and the next update clamps it. Fix cheaply at **Step 29** by batching one catalogue read into the merge |
| 2026-09-06 | Step 13 | **`GET /store/cart` writes.** It is the one read on the platform that saves, because merge-on-login must happen whether or not the storefront called the merge endpoint | ℹ️ Deliberate and documented in the handler. Recorded so it is not mistaken for a leak of a write into a query |
| 2026-09-06 | Step 13 | **`GET /store/checkout/{id}/payment-methods` runs the quote twice** — once as prepaid and once as cash on delivery — so the COD fee shown is the fee that will be charged rather than a second reading of the settings section | ℹ️ Deliberate: this module computes no money. Revisit only if the endpoint shows up in a latency profile |
| 2026-09-06 | Step 13 | **The `QuoteResult` snapshot is stored through a JSON value converter, not EF's owned-JSON mapping.** A field added to the contract will not be caught by a migration, and an old snapshot deserialises with the new field defaulted | ℹ️ Deliberate — the quote is another module's contract type and must not become an entity graph this module can query into. **Step 29 needs a round-trip test**; a breaking change to `QuoteResult` needs a migration of stored snapshots |
| 2026-09-06 | Step 13 | **Nothing retires `Expired` carts.** The sweeper moves rows into `Expired` and they stay there for ever | ⏳ **Step 31** housekeeping, alongside the audit-log and notification-log retention already parked there |
| 2026-09-06 | Step 13 | **`CART_COOKIE_SECURE` added to `.env.example` and wired through compose**, false in dev for the same reason `AUTH_REFRESH_COOKIE_SECURE` is: a browser will not return a `Secure` cookie over the dev stack's plain http and the basket would appear to empty itself | ℹ️ **Never false in staging or production.** Step 32 must assert it |
| 2026-09-06 | Step 13 | **Guest checkout is switched off**, not absent: `CartsOptions.RequireSignInToCheckout` defaults to true because an order needs somebody to send it to and somebody to answer for it | ℹ️ Recorded as a switch rather than an assumption so the decision is visible. Turning it on would need a guest identity, which is Phase 2 |
| 2026-09-06 | Step 14 | **Inventory has no consumer for `Orders.SubOrderCancelled`, so units committed out of stock are never put back.** A cancellation *before* confirmation releases the cart-scoped reservation directly and is complete; one *after* confirmation publishes the event with the quantities and nothing listens | ⏳ **The single most important item on this list.** `02-domain-model.md` §6 assigns the consumer to Inventory. Needs either a handler in the Inventory module or a `RestockAsync` on `IStockAvailability`. Raise at the Step 14 boundary; it must not reach Step 28A unowned |
| 2026-09-06 | Step 14 | **`IPaymentInitiation` is a new contract this step invented**, in `KlaraHome.Contracts/Payments`, mirroring the `IOrderPlacement` seam Cart declared at Step 13. Orders registers a refusal for it, so **prepaid placement answers 503 `PAYMENTS_UNAVAILABLE` until Step 15**. Cash on delivery works end to end today | ℹ️ Deliberate and the established pattern. **Step 15 must implement this interface rather than inventing its own seam**, and must not create a second idempotency store |
| 2026-09-06 | Step 14 | **`QuoteRequest.IsFirstOrder` is still always false.** Step 13 parked this for Step 14 to supply, and Orders does not yet expose a contract Cart could ask | ⏳ **Not delivered.** Needs an `IOrderHistory`-shaped contract on `KlaraHome.Contracts/Orders` and a call from `CartRenderer`. Cheap; raise at the Step 14 boundary |
| 2026-09-06 | Step 14 | **Loyalty accrual on completion is not written.** `PricingSettings.LoyaltyAccrualPercent` and `LoyaltyMaxAccrualPerOrder` document the accrual as *Orders'* to write when an order completes, and Step 14's deliverables do not name it | ⏳ Out of this step's scope, so parked rather than built. The completion sweeper is the natural place. Raise at the Step 14 boundary |
| 2026-09-06 | Step 14 | **Store credit still cannot be applied at checkout** (carried from Step 13), so `orders.wallet_applied` is written faithfully from the quote and is always zero in practice | ⏳ Unchanged. Needs the `PUT /store/checkout/{id}/wallet` endpoint Step 13 parked. The order side is complete |
| 2026-09-06 | Step 14 | **A promotion whose last use is taken between quoting and placing is honoured anyway.** `IPromotionLedger.RedeemAsync` reports the refusal, and the placement records a note rather than re-pricing the order | ℹ️ Deliberate: the shopper agreed to a total, and charging more than the confirmation says is worse than honouring a coupon once too often. The discrepancy is visible in the campaign report. Confirm the commercial call at the boundary |
| 2026-09-06 | Step 14 | **The order's money movers are not in one transaction with the order.** Promotion redemption and the store-credit debit are written by Pricing's context; the placement compensates explicitly on every failure path and rolls its own transaction back | ⏳ Accepted for the sprint, and the same shape Step 13 accepted for the stock hold. **Step 29 must prove every compensation runs** |
| 2026-09-06 | Step 14 | **The stock commit sits inside the placement transaction.** If the commit succeeds and the order then fails to save, units are lost from supply until an operator corrects them | ℹ️ Deliberate, and the safe direction: the other ordering would release units for an order that did save, which is an oversell. Recorded so the asymmetry is visible |
| 2026-09-06 | Step 14 | **A vendor caller loading an `Order` sees the order shell with only their own sub-orders**, because the vendor query filter is on `sub_orders` and not on `orders`. Every handler that could leak the shell checks for an empty collection and answers 404 | ℹ️ Documented in `OrdersDbContext`. **Step 29's authorisation matrix must prove it** for every order route |
| 2026-09-06 | Step 14 | **`docs/04-api-specification.md` has no §4 (admin surface) at all**, although Steps 9-14 have all referenced one. Every admin route built since Step 9 is specified only in code | ⏳ Pre-existing documentation debt, not introduced here. Belongs with the OpenAPI-generation work; raise at the Step 22 boundary at the latest |
| 2026-09-06 | Step 14 | **`platform.idempotency_keys` (§4.1) is still not created** and Step 4 said Step 13/14 at the latest. Orders is idempotent by delegation — Cart's `checkout_placements` row is what makes place-order at-most-once — so Orders needed no store of its own | ℹ️ Reasonable, and the reason the table is still absent. **Step 15 is the last honest deadline**: refunds and webhooks need it |
| 2026-09-06 | Step 14 | **Invoices are raised at `Packed`, not at `Confirmed`.** `02-domain-model.md` does not say which, and the choice is commercial: invoicing at confirmation means a credit note for every seller who later cannot fulfil | ℹ️ Configurable (`Orders:AutoInvoiceOnPacked`) and reversible by hand through `POST /admin/sub-orders/{id}/invoice`. Confirm the choice at the boundary |
| 2026-09-06 | Step 14 | **A cancelled invoice has no credit note.** `Invoice.Cancel()` exists and nothing calls it; reversing the money is the Returns module's | ⏳ **Step 17** owns the credit note. The invoice side is ready for it |
| 2026-09-06 | Step 14 | **`orders.payment_status` is a mirror with nothing writing to it** until Payments exists. Every order reads `Pending` | ⏳ **Step 15.** The column, its constraint and its filter are in place |
| 2026-09-06 | Step 14 | **Nothing consumes `Orders.OrderPlaced`, `SubOrderConfirmed`, `SubOrderStatusChanged`, `InvoiceIssued` or `OrderCompleted` yet.** They are written to the outbox and dispatched to no handler | ℹ️ Expected — the consumers are Payments (15), Shipping (16), Settlements (18), Notifications and Reporting. Recorded so the silence is not mistaken for a bug |
| 2026-09-06 | Step 14 | **The `en-IN` culture is required at runtime for invoice rendering.** `InvoiceDocumentBuilder` calls `CultureInfo.GetCultureInfo("en-IN")`, which throws on a globalisation-invariant container | ⏳ **Step 32 must not set `InvariantGlobalization=true`** on the API or worker image, or invoices will fail to render. Verify at Step 28A |
| 2026-09-06 | Step 15 | **`docs/04-api-specification.md` was truncated by an earlier uncommitted edit.** Everything from §3.5 to §7 — the admin endpoint list, the webhook contract, cross-cutting behaviours and the OpenAPI section — had been deleted from the working copy | ✅ **RESTORED** from `HEAD` and re-numbered. But `HEAD` is the **Step 8** version, so the §4 admin-route expansions written during Steps 9–13 are gone and cannot be recovered from git. Re-deriving them from the code is a documentation task; raise it at the boundary |

| 2026-09-06 | Step 15 | **No Razorpay credentials exist.** `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET` and `RAZORPAY_WEBHOOK_SECRET` are blank | ⏳ By the User's instruction: everything is wired and blank, and filling three values into `.env` turns payments on with no rebuild. Nothing was proved against a live gateway, so every sandbox row in `TEST_DEBT.md` stays open |

| 2026-09-06 | Step 15 | **`IOrderPaymentSync` was added to `KlaraHome.Contracts` and implemented inside the Orders module.** Payments needs a way to confirm and fail an order, and `01-architecture.md` §2.1 calls for a synchronous call rather than an event | ℹ️ A shared-layer addition made during this step, in the same shape as the seams added at Steps 12 and 13. Synchronous deliberately: the webhook handler must know whether the order confirmed before it marks the event processed |

| 2026-09-06 | Step 15 | **A `payments` store-settings section was added** — `refundApprovalThreshold`, `autoRefundOnCancellation`, `preferInstantRefunds`, `codRemittanceGraceDays` | ℹ️ The same split every module draws. When a refund needs a second signature is a governance decision a business revisits after an incident, and it must not need a deploy |

| 2026-09-06 | Step 15 | **`tools/ef.sh` was missing `Orders`** — Step 14 added it to `ef.ps1` and not to the shell script, so `./tools/ef.sh add Orders …` would have failed | ✅ Fixed alongside adding `Payments` to both. A one-word repair, recorded rather than smoothed over |

| 2026-09-06 | Step 15 | **Nothing consumes `PaymentCaptured`, `PaymentFailed`, `RefundProcessed`, `CodCashRecorded` or `PaymentMismatchDetected`.** They are written to the outbox and dispatched to no handler | ℹ️ Expected — the consumers are Notifications (the receipt and the failure), Settlements (18) and Reporting (21). **`PaymentMismatchDetected` is the one that matters**: until something carries it, a reconciliation mismatch is a log line rather than an inbox |

| 2026-09-06 | Step 15 | **Vendor payouts are not built.** `Razorpay__RouteEnabled` is read and recorded and nothing acts on it; `IVendorPayoutAccounts` still provisions nothing | ⏳ **Step 18** owns Route and RazorpayX. Recorded because the configuration now exists and could be mistaken for a working payout path |

| 2026-09-06 | Step 15 | **A cash-on-delivery refund has no route out.** `InternalCodPaymentProvider.RefundAsync` refuses with `REFUND_COD_NOT_AUTOMATIC`, and there is no bank-transfer or store-credit path behind it | ⏳ **Step 17** owns it. The refusal names the reason rather than pretending to succeed |

| 2026-09-06 | Step 15 | **`cod_collections.shipment_id` is never set.** There are no shipments until Step 16, so cash is attached to a sub-order and not to a parcel | ⏳ **Step 16.** `AttachShipment` exists and nothing calls it |

| 2026-09-06 | Step 15 | **The reconciliation sweep and the event processor bypass the tenant query filter** — they are the platform draining its own inbox and have no ambient tenant | ℹ️ The same arrangement the outbox dispatcher and every sweeper before them use. Correct for single-tenant-per-deployment (the v1 model), and must be revisited if that ever changes |

| 2026-09-06 | Step 15 | **Payments has no vendor scope at all**, unlike every other schema that carries seller data | ℹ️ Deliberate, and stated in `PaymentsDbContext`: a payment is made against an order that may span two sellers, so there is no seller it belongs to. What a seller may see about money is their settlement, which is Step 18's |

| 2026-09-06 | Step 15 | **A settlement line that is not a `payment` is marked `Matched` without being matched to anything.** A fee adjustment or a Route transfer has no payment of ours behind it | ℹ️ "Matched" here means "accounted for". Calling them unmatched would fill the mismatch report with noise nobody can act on — but the word is doing two jobs, and Step 18 may want a third state |

| 2026-09-06 | Step 15 | **No Polly resilience pipeline on the gateway client.** `08-integrations.md` asks for timeout → retry with jitter → circuit breaker; this build has the bounded timeout and the host allow-list, and no retry or breaker | ⏳ Deliberate for the sprint: a blind retry on a *money-moving* POST is worse than none, and the safe retries already exist as the reconciliation sweep. **Step 29 or 31** should add a breaker on the read paths |

| 2026-09-06 | Step 15 | **No health check for the gateway.** `08-integrations.md` names one as part of the common adapter contract | ⏳ **Step 31** owns observability and probes. An unconfigured gateway would otherwise make a fresh deployment report unhealthy, which is wrong |

| 2026-09-06 | Step 15 | **`Razorpay__AccountNumber` is captured and unused.** The settlement API accepts it and this build does not send it | ℹ️ Harmless — settlement ingestion works without it on a single-account merchant. Recorded because it is one of the four values `06-infrastructure-devops.md` publishes |

| 2026-09-06 | Step 15 | **The webhook route is `/api/v1/webhooks/razorpay` and is provider-named.** A second gateway would need a second route rather than a `{provider}` segment | ℹ️ Correct as written: the signature header, its algorithm and the body shape are all provider-specific, and a generic route would have to sniff which before it could verify anything |

| 2026-09-06 | Step 15 | **`AutoCapture` is on, so an `Authorized` payment is rare** — and the manual-capture endpoint therefore has almost no exercise | ℹ️ Kept because a gateway that cannot auto-capture still authorises, and an authorisation nobody can capture is money held against an order nobody can fulfil |

| 2026-09-06 | Step 16 | **Three contracts added to `KlaraHome.Contracts`**: `IOrderFulfilment` (Orders), `ICodCollections` (Payments) and `IVendorPickupPoints` (Vendors). A parcel is booked from an order's address and lines, settles a cash record, and leaves from a seller's registered pickup point — and no foreign key may cross a schema | ℹ️ Shared-layer addition, unavoidable and the mirror of `IOrderPaymentSync`. `IVendorPickupPoints` is the **only place another module writes into the Vendors schema** — one field, the courier's id for an address, which only Shipping can learn |
| 2026-09-06 | Step 16 | **Nothing consumes `Shipping.ShipmentDispatched`, `ShipmentTrackingUpdated`, `ShipmentDelivered` or `ShipmentNdrRaised`.** They reach the outbox and stop there, so the "customer notifications" half of the step's deliverable is not delivered: the shopper is told nothing when their parcel moves | ⏳ The consumer is Notifications, and it needs four templates and one handler. Belongs with the identical gap already parked for Orders (14) and Payments (15) — **Step 21 or Step 29**. The order timeline *is* written, so "where is my order" is answerable; nobody is pushed |
| 2026-09-06 | Step 16 | **The rendered fallback shipping label has no barcode.** The shared document vocabulary draws text, rules and tables only, so the air waybill is printed large and read by a human | ℹ️ Adequate for a hand-booked parcel, and an aggregator's own label always wins where one exists. A barcode would mean a new `DocumentBlock` and an image encoder — revisit only if a client ships at volume without an aggregator |
| 2026-09-06 | Step 16 | **`shipping.tracking_events` partition maintenance is not scheduled.** Twenty-five months exist from first migration; after that, scans land in the default partition and stay correct but unpartitioned | ⏳ **Step 31**, with `platform.audit_logs`, `inventory.stock_ledger_entries` and `notifications.notification_messages`. Note the retention difference: tracking scans are evidence about a delivery and a dispute can arrive months later, so the job must create partitions and drop them only on a long horizon |
| 2026-09-06 | Step 16 | **The `dotnet format` gate was already red before this step.** Import ordering in `Inventory/StockFeature.cs` and `Carts/CheckoutFeature.cs`, and a missing `Async` suffix on `AdminPaymentFeature.Load` | ✅ **RESOLVED** — `dotnet format` was run across the solution and the method renamed to `LoadAsync`. Out of Step 16's scope, and fixed rather than parked because a red gate hides the next failure. `./tools/ci.sh format` now passes |
| 2026-09-06 | Step 16 | **Freight tax is computed twice, from the same figure.** Shipping back-calculates it at a configured 18 % for display; the pricing engine splits the same tax-inclusive amount for the invoice | ℹ️ Deliberate. Both derive from one inclusive number, so they cannot disagree about a total — only about the split, and the invoice's is authoritative. Revisit if a deployment ever needs a freight rate other than the standard one |
| 2026-09-06 | Step 16 | **A seller's pickup address is registered with the aggregator under its label**, because this class of API takes a name rather than an address on a booking. A seller with two locations called the same thing would book both from one | ⏳ Needs an explicit registration call once a sandbox account exists — **Step 29**, with the rest of the aggregator contract testing. The failure is visible (a courier collects from the wrong door) rather than silent. **Step 16A** names the aggregator (Shiprocket) but does not change this: the registration call still belongs with the rest of the sandbox contract testing |
| 2026-09-06 | Step 16 | **The seeded rate card is one catch-all zone in three weight bands.** Metro and remote zones are not seeded, because their PIN-code maps differ per deployment and a guessed map prices parcels wrongly while looking configured | ℹ️ Deliberate. A store has a working tariff on day one and four rows to edit; the map is an operator's decision |
| 2026-09-06 | Step 16A | **`ShippingProviders.Aggregator` is retired as a selector and kept as a legacy alias.** Step 16 booked every parcel under the key `aggregator`, which records the *class* of courier rather than the courier | ℹ️ Deliberate compatibility, not indecision. Nothing is deployed, so no row carries the old key today; the alias costs one line and means a configuration file written last week still resolves. Remove it at **Step 28A** if nothing depends on it |
| 2026-09-06 | Step 16A | **No operator override ships a parcel outside the coverage area.** A store restricted to Hyderabad cannot take a one-off order to Chennai even when it wants to | ⏳ Out of scope by decision (ADR-018). It is a permission and an audit trail rather than a settings field, and nobody has asked for it yet. Raise it again the first time an operator asks, which is the point at which its shape becomes knowable |
| 2026-09-06 | Step 16A | **The coverage rule is written twice** — once in `DeliveryCoverageService.Covers` and once in the Cart module's degenerate `StandardShippingOptions`. The two modules may not reference one another, and a shared helper would have to live in `KlaraHome.Contracts`, where behaviour does not belong | ℹ️ Deliberate, and bounded: ten lines of pure matching over a settings record, and the Cart copy is unreachable the moment the Shipping module is registered — which it always is in a real deployment. If a third implementation ever appears, the rule belongs on the settings record itself |
| 2026-09-06 | Step 16A | **The delivery area is edited through `PUT /admin/settings/delivery-coverage`, not through a shipping route.** `docs/04` §4 originally named `PUT /admin/shipping/coverage` | ✅ **RESOLVED in the specification.** A second write path for one settings row would be a second place to get the audit trail and the validator wrong. Shipping reads it and tests it; Platform owns the write, as it does for every other store policy |
| 2026-09-06 | Step 16A | **The coverage check runs on every cart render**, adding a settings read and — for a PIN code the platform has no row for — one indexed query against `shipping.serviceability_cache` | ⏳ Both are cheap and the settings read is cached by the Platform module, but the cart render is the hottest authenticated path there is. **Step 29** should measure it; if it shows, the answer is to resolve the place once per checkout session rather than to weaken the gate |
| 2026-09-06 | Step 16A | **`IReferenceData.PincodeAsync` queries rather than caching.** The states are 36 rows and are held in memory; the PIN codes are roughly nineteen thousand and are not | ℹ️ Deliberate — caching them all in every process to answer one lookup would trade a millisecond for tens of megabytes, and the unique index on `code` makes this a point read. Revisit only if a profile says otherwise |
| 2026-09-06 | Step 16A | **Shiprocket's API paths are written from its published documentation and have never been called.** No account exists (`08-integrations.md` §7) | ⏳ Recorded on the step card and in the adapter's own remarks: **confirm every path on the day the account is provisioned.** Until then `Shipping:Provider` stays blank, the registry hands bookings to the manual adapter, and nothing here is exercised |
| 2026-09-06 | Step 16A | **`ShiprocketWire` still carries the HMAC verification Step 16 wrote**, even though Shiprocket proves origin with a header secret | ℹ️ Kept on purpose. The adapter accepts either, so a deployment behind a signing proxy works, and the next courier's adapter — which may sign properly — needs no new primitive |
| 2026-09-06 | Step 17 | **A replacement is recorded, not placed.** `POST /admin/returns/{id}/replace` takes a `replacementOrderId` an operator supplies; nothing in this module can create the replacement order | ℹ️ Deliberate. Placing an order from inside Returns would mean a returns queue that can create sales — a great deal of authority for a workflow whose whole job is deciding what is owed. **Step 27/28 should give the admin app a "reorder these lines" action** that places the order and hands the id back here |
| 2026-09-06 | Step 17 | **`FinancialYear` and the gapless `NumberSequence` are now written twice** — once in Orders, once in Returns — because a module may not reference another and neither belongs in `KlaraHome.Infrastructure` | ℹ️ The module boundary's price, and the same trade the `PermissionCatalog` duplication makes. The two must agree exactly: a credit note filed into a different financial year from the invoice it credits is a mismatch a GST return finds. **Step 29 should assert the two implementations agree** on an April boundary |
| 2026-09-06 | Step 17 | **A scrapped return line writes a ledger entry of zero.** The units never went back on supply, so there is nothing to subtract; the entry exists only to record that they were accounted for and destroyed | ⏳ Correct as far as the ledger's own arithmetic goes, and arguably a `damage` movement should be visible as a quantity somewhere. **Step 21's reporting read-models** should decide whether scrapped-on-return is a figure worth surfacing; if it is, it needs a column rather than a zero row |
| 2026-09-06 | Step 17 | **`ReturnsSettings.PickupSlaDays` drives the stale-return sweep, which reports and never repairs.** An overdue collection, an uninspected parcel and an unpaid refund all become warnings in the log | ⏳ Deliberate — re-booking a courier automatically would send a second van, and refunding automatically would move money nobody looked at. But a log line is not an inbox. **Step 31 (observability) should turn these three into alerts**, and Step 28 should give the admin app a queue view keyed on them |
| 2026-09-06 | Step 17 | **`IVendorDirectory` gained `ReturnPolicyAsync`**, reading the seller's existing `return_policy` document. It is a second call rather than a field on `VendorSummary` | ℹ️ Deliberate: the summary is read on the buy-box path for every listing on a page, and this is read once when somebody asks to send something back. No schema change was needed — the column already existed from Step 9 |
| 2026-09-06 | Step 17 | **A return-to-origin parcel is restocked without inspection.** The `RtoDelivered` scan puts every live unit back on supply | ℹ️ Correct for the ordinary case — an RTO parcel went out, nobody took it, and it came home sealed. A store that wants them inspected has no switch for it today. Raise with the User if a client asks; the disposition machinery already exists and would only need a settings flag |
| 2026-09-06 | Step 17 | **Notifications has no templates for any of the seven `Returns.*` events.** They are published to the outbox and nothing subscribes | ⏳ The same gap `Shipping.*` had at Step 16. It belongs with the rest of the transactional messaging work; **Step 28A or a Notifications pass should add the templates** — a shopper whose return was approved is currently told only by the order timeline |
| 2026-09-06 | Step 17 | **`Returns.ReturnQcCompleted` is published and nothing consumes it.** Inventory acts through the synchronous `IStockRestock` seam instead, inside the same request | ℹ️ Deliberate and the safer of the two: a restock that happened asynchronously could not report failure to the operator who graded the goods. The event stays for Settlements (Step 18) and Reporting (Step 21), which genuinely are asynchronous consumers |
| 2026-09-06 | Step 18 | **`Payments.RefundProcessed` is deliberately not consumed.** This ledger reverses *supplies*, and the two documents that undo a supply are a cancellation and a credit note; a refund is money moving in response to one of them | ℹ️ Decided. Subscribing to all three would reverse the same sale twice and hand the seller back the commission twice. It does mean a refund paid **without** either document — a goodwill payment raised straight from the Payments surface — never reaches the seller's statement, and correcting one is a manual adjustment. Raised at the boundary; a `TEST_DEBT` row guards the decision |
| 2026-09-06 | Step 18 | **A cash-on-delivery sale earns only when the courier remits, and remittance is recorded by hand.** `POST /admin/cod/remittances` is an operator action, so a seller's cash earnings arrive when somebody keys the courier's statement in | ℹ️ Correct: the money is not the platform's until it is remitted, and paying earlier would be paying out of pocket. The manual step is Step 16's, not this one's, but it is what governs when a COD seller is paid |
| 2026-09-06 | Step 18 | **`FinancialYear` and the gapless `NumberSequence` are now written three times** — Orders, Returns and Settlements | ℹ️ The module boundary's price, paid a third time and for the last time in Phase D. The arithmetic must agree exactly across all three: a TCS extract filed against a different financial year from the invoices it covers is a mismatch a GST return finds. A shared implementation would be infrastructure holding a table three modules write to |
| 2026-09-06 | Step 18 | **A minimal RFC 4180 CSV writer is now written twice** — once in Catalog for the product export, once here for the statement and the TCS/TDS extract | ℹ️ Forty lines each, for the same boundary reason. Worth revisiting at Step 29 only if a third module needs one |
| 2026-09-06 | Step 18 | **Nothing consumes `Settlements.SettlementCycleClosed`, `PayoutCompleted` or `PayoutFailed`.** They reach the outbox and stop there | ⏳ The same gap `Shipping.*` and `Returns.*` have. Notifications has no templates for them, so a seller is not yet told their statement is ready or that they have been paid. Step 21's reporting read-models and a Notifications template pass close it |
| 2026-09-06 | Step 18 | **`IVendorPayoutAccounts` is still unimplemented, so no seller has a `gateway_account_id`.** Step 9 left the seam with a no-op that creates nothing and said Step 18 would fill it | ⏳ **Not done, and named here rather than quietly skipped.** The seam is internal to the Vendors module, so the implementation belongs there and would need a second Razorpay HTTP client inside it; with `Payouts:Provider` blank by the User's instruction nothing could be proved against a gateway either way. The consequence is concrete: Route payouts cannot succeed until a linked account exists per seller, and every payout item would be recorded `Skipped` with "the seller has no payout account at the gateway". Raised at the boundary |
| 2026-09-06 | Step 18 | **The Route adapter never returns a UTR.** A Route transfer moves money inside the gateway; the seller's own bank reference belongs to *their* settlement, which this platform does not read | ℹ️ Left null on purpose. Inventing one from the transfer id would put a number on a statement no bank has heard of. A deployment that needs a bank reference on every payout uses the RazorpayX rail, which does carry one |
| 2026-09-06 | Step 18 | **`transfer.processed` / `transfer.failed` webhooks are not received.** The Payments module owns the Razorpay webhook endpoint and does not know these event types, so a payout's outcome is learnt only by the fifteen-minute reconciliation sweep | ⏳ Acceptable and slower than it needs to be. Closing it means either a Route handler inside the Payments module or a second webhook route here, and neither can be tested without credentials |
| 2026-09-06 | Step 18 | **The platform's own commission invoice is not rendered.** The `platform_tax` entry records the GST the platform charged a seller, but no document is produced for the seller to claim input credit against | ⏳ Real and out of scope. A marketplace has to raise its own tax invoice for commission; the numbers exist, the PDF does not. Sizeable enough to be its own step |
| 2026-09-06 | Step 18 | **The nightly balance-assertion job `03-database-design.md` §4.12 names is not written.** `Σ credits − Σ debits = closing balance` is asserted nowhere but in tests that do not exist yet | ⏳ Step 29's, and recorded in `TEST_DEBT.md` as the step's headline row |
| 2026-09-06 | Step 18 | **`SettlementSettings.TdsAnnualThreshold` defaults to zero — deduct from the first rupee.** The statutory ₹5,00,000 relief applies only to an individual or a Hindu undivided family who has furnished a PAN or Aadhaar, and the platform holds no field saying which sellers those are | ℹ️ Deliberately conservative: a short deduction is the platform's liability, an over-deduction is the seller's credit. An operator who has established their sellers qualify sets the threshold. A `business_type` on the vendor record would let it be resolved per seller; that is a Vendors change and not this step's |
| 2026-09-06 | Step 18 | **A seller in deficit is carried, never invoiced.** A period whose returns exceeded its sales produces a negative `net_payable`, which becomes the next period's opening balance | ℹ️ Correct and worth stating: the platform has no mechanism to demand money back from a seller, and a persistent negative balance is a commercial conversation rather than a system action |
| 2026-09-06 | Step 18 | **A payout batch's `total_amount` does not move after approval**, so an item skipped during processing leaves the headline figure larger than what was sent | ℹ️ Deliberate. The total is the number somebody signed against; `settled_amount` is what actually left. A document whose total changed after signature is a document nobody can audit |
| 2026-09-06 | Step 18 | **The commission fallback resolves one quote for a whole sub-order** when any line was frozen without a commission, using the first such line's category and unit price | ℹ️ An approximation on a path that should not be reachable — a line has a commission unless the seller had no plan when the order was placed. The alternative, charging nothing, is worse; the honest one, resolving per line, is several more calls on a path nobody hits |
| 2026-09-06 | Step 18 | **Two new contracts added to `KlaraHome.Contracts`**: `IOrderSettlement` (Orders) and `IVendorPayouts` (Vendors). The first is the only one of ordering's four outward seams that cannot write | ℹ️ Intentional. A settlement run must not be able to change the sale it is settling, and the read-only seam is what makes that a property rather than a promise |
| 2026-09-06 | Step 18 | **The Finance and Vendor-owner role bundles gained the Returns permissions Step 17 declared but never granted.** Step 17 added its five permissions to the catalogue and to no role | ℹ️ Repaired here because the two lists were visibly out of step while adding the settlement permissions beside them. Finance gained `returns.return.read` and `returns.refund.manage`; the vendor owner gained `returns.return.read` and `returns.return.manage` |
| 2026-09-06 | Step 18 | **Four domain types were made `internal` after the architecture test caught them public** — `LedgerDirection`, `LedgerEntryTypes`, `LedgerReferenceTypes` and `SettlementCycleStatus` | ℹ️ Fixed in the step. Recorded because it is the first time `A_module_exposes_nothing_publicly_except_its_module_class` has fired, and it fired for the right reason |
| 2026-09-06 | Step 18 | **The `dotnet format` gate is clean for every project this step touched**, including the generated EF migration — whose line endings were normalised to LF by hand, because `dotnet ef` writes CRLF and `.editorconfig` asks for LF | ℹ️ Fixed for this module only. Every earlier module's generated migration is still CRLF, so the solution-wide gate stays red on the two files Step 16 recorded plus those. Step 28A or Step 29 |
| 2026-09-06 | Step 19 | **A new shared contract added to `KlaraHome.Contracts`: `IProductProjectionSource` (Catalog).** It is wider than `IProductCatalog` — names, slugs, attribute labels, the picture — and it resolves the buy box on the catalogue's side | ℹ️ Intentional, and the buy-box half is the point: a consumer that picked its own winner would eventually link a search result to a page showing a different seller at a different price. Reporting (Step 21) is its expected second consumer |
| 2026-09-06 | Step 19 | **`search_stop_words` is a table `03-database-design.md` §4.13 did not name.** The section listed `search_synonyms` and `search_queries`; the deliverable also asked for stop words | ℹ️ §4.13 rewritten to describe the schema as built, and recorded in `CHANGE_LOG.md`. PostgreSQL's `english` dictionary already drops grammatical stop words; this table is for the store's own noise — "buy", "online", its own brand name |
| 2026-09-06 | Step 19 | **`product_search_projection` gained a `category_ids uuid[]` column §4.13 did not name**, derived from `category_path` and GIN-indexed | ℹ️ Necessary rather than optional: filtering by a parent category has no good answer otherwise — a prefix match on the path needs the caller to know the ancestors, and a substring match cannot use an index. Recorded in `CHANGE_LOG.md` |
| 2026-09-06 | Step 19 | **Phrase synonyms are refused.** A synonym's term and each of its expansions must be a single word of letters or digits | ℹ️ Deliberate. A phrase synonym needs `tsquery`'s phrase operator and a different index; half-supporting it — by matching the first word — would be worse than refusing it, because the rule would appear to work and quietly mean something else. Build it if merchandising asks |
| 2026-09-06 | Step 19 | **The vocabulary cache is per-process with a sixty-second TTL, not a distributed invalidation.** An edit in the API process is seen by the worker up to a minute later | ℹ️ Accepted trade. The alternative is a cache-invalidation channel bought for a benefit measured in seconds of staleness on a merchandising change. The admin screen should say how long it takes (Step 28) |
| 2026-09-06 | Step 19 | **The category facet keeps its own filter while every other facet has its lifted.** It is a drill-down rather than a multi-select | ℹ️ Deliberate and documented in `04-api-specification.md` §3.2: inside Furniture a shopper wants the sofas and the tables beneath it, not every category in the store |
| 2026-09-06 | Step 19 | **Attribute facet counts are capped globally in SQL and trimmed per group in C#**, because the statement cannot know how many distinct attribute keys it will find | ℹ️ Works, and is a place a very wide catalogue could starve a rarely-used attribute of its values. Revisit at Step 29 with a real 50,000-SKU catalogue if a facet looks short |
| 2026-09-06 | Step 19 | **The fuzzy pass sets pg_trgm's session threshold with `set_limit` on the pooled connection.** Session state on a pooled connection is normally worth avoiding | ℹ️ Safe here — it is set immediately before every query that reads it, and no other query in the platform uses the `%` operator that consults it. The alternative, an explicit `similarity()` predicate alone, cannot use the trigram index |
| 2026-09-06 | Step 19 | **Ratings on the projection are always null today.** `RatingAverage` and `RatingCount` are copied from the catalogue, which has no reviews until Step 21, so the rating facet and the rating sort are wired and empty | ⏳ Expected. Step 21 publishes `Reviews.ReviewPublished`; **this module will need to subscribe to it**, and that subscription is Step 21's to add |
| 2026-09-06 | Step 19 | **The index is empty until something rebuilds it.** A deployment that installs Step 19 over an existing catalogue has no rows until an operator calls `POST /admin/search/index/rebuild` or the worker's sweep runs — and the sweep only refreshes rows that already exist | ⏳ Raise at Step 28A. The honest options are a first-run rebuild triggered by the migrator, or a documented deploy step. It is deliberately not a seeder: seeders are for data a business could not delete |
| 2026-09-06 | Step 19 | **The eight compiler warnings in `KlaraHome.Modules.Payments` are still open**, and `TreatWarningsAsErrors` is on when `CI=true` — so a CI build of the solution fails on them today | ⏳ Not this step's to fix (protocol rule 6). Two nullability, three analyzer style, one unread primary-constructor parameter. **Step 28A** — they are exactly the kind of drift that step exists to clear |
| 2026-09-06 | Step 19 | **`docs/01-architecture.md` §9 was missing ADR-018.** Step 16A wrote the ADR file and the `adr/README.md` row but not the architecture table row | ℹ️ Added while adding ADR-019 beside it, because the two lists were visibly out of step. A one-line correction with no decision in it |
| 2026-09-06 | Step 19 | **Popularity counts a confirmed sale and is never decremented by a cancellation or a return.** `SubOrderCancelled` and the credit-note events are not consumed here | ℹ️ Deliberate for now: popularity is a ranking hint rather than a ledger, the score is logarithmic so a handful of reversals move it very little, and subscribing would need the same inbox-keyed idempotency for a second event. Revisit if returns ever run high enough to distort ranking |
| 2026-09-06 | Step 19 | **The search index holds no seller-scoped view.** Every row is every seller's, and the `search` schema has no vendor query filter | ℹ️ Correct rather than an omission: the projection is what a shopper searches, so it must contain every seller's offers. A seller's view of their own listings is the catalogue's question, and the catalogue's tables are scoped for it |
| 2026-09-06 | Step 19 | **`GET /store/products` reads its filters off the query string by hand** rather than binding them with `[AsParameters]` | ℹ️ Forced by the attribute filters: `attr.color` is a name a merchandiser invents, and no record type can declare a parameter for it. Everything else is read the same way so there is one place that says what the endpoint accepts |
| 2026-09-06 | Step 19 | **The projection clamps MRP up to the price when Pricing returns a price above it**, rather than failing the row | ℹ️ Should be unreachable — the catalogue refuses an offer above its MRP and so does the law — but an index that refused to build because one product's price list was misconfigured would take the storefront's whole search down with it |
| 2026-09-06 | Step 19 | **Search suggestions read the query log, so a fresh store suggests nothing until people have searched it** | ℹ️ Expected and self-correcting. The product, brand and category branches answer from the first day; the popular-query branch fills in as the store is used |
| 2026-09-06 | Step 20 | **`IProductProjectionSource` gained two methods, and a new sibling contract `ICatalogTaxonomy` was added.** `FindByProductsAsync` (a collection holds product ids; every read-model is keyed on the variant) and `FindBySlugAsync` (a crawler asks for a URL, and a URL carries a slug) | ℹ️ Intentional. Both keep the buy-box resolution on the catalogue's side, which is the property that stops a collection tile and the product page it links to naming two sellers. `ICatalogTaxonomy` is the third and narrowest seam onto `catalog` — the tree, for a sitemap and a breadcrumb |
| 2026-09-06 | Step 20 | **Brands are deliberately absent from `ICatalogTaxonomy` and from the sitemap.** They have slugs and would have been trivial to add | ℹ️ The storefront route map (`05-frontend-architecture.md` §3.2) has no route that serves a brand. Listing them would put URLs in a sitemap that answer 404, which is worse for a crawler's opinion of the site than listing nothing. Revisit if Step 24 adds a brand page |
| 2026-09-06 | Step 20 | **A new store-settings section, `seo`, was added to `KlaraHome.Contracts` and to the Platform settings catalogue** — canonical origin, indexing switch, robots directives, title template, organisation identity and the sitemap toggles | ℹ️ Settings rather than configuration for the reason the rest of that list is: a staging site that has leaked into an index is a problem measured in minutes, not in releases. `AllowIndexing` ships **off**, so a new environment is private until somebody decides otherwise |
| 2026-09-06 | Step 20 | **`banners` gained a `message` column and an `AnnouncementBar` placement that `03-database-design.md` §4.14 did not name.** The announcement bar was a Step 20 deliverable with no table | ℹ️ §4.14 rewritten to describe the schema as built, and recorded in `CHANGE_LOG.md`. It is the same record because it answers the same three questions — what does it say, when does it run, who sees it — and a second table would have needed its own scheduling, targeting and admin screen to answer them again |
| 2026-09-06 | Step 20 | **`collection_items` gained an `is_from_rule` column §4.14 did not name** | ℹ️ Load-bearing rather than convenient: without it, refreshing a rule-based collection would either delete an editor's pins or accumulate every product that had ever matched. Recorded in `CHANGE_LOG.md` |
| 2026-09-06 | Step 20 | **This module knows the storefront's URL shapes** (`/pages/{slug}`, `/collections/{slug}`, `/c/{slug}`, `/p/{slug}`), confined to one file, `StorefrontRoutes` | ℹ️ Unavoidable: a sitemap and a `schema.org` graph are both lists of URLs a crawler will fetch, and there is no way to produce one without knowing what they are. The alternative — the Angular app generating its own sitemap — would mean the storefront enumerating the catalogue. Named rather than hidden |
| 2026-09-06 | Step 20 | **`/blog` and `/blog/{slug}` are not in the storefront route map.** The blog is a Step 20 deliverable marked optional and is behind `content.blog`, which ships off | ⏳ **Step 23 or 24 must add the two routes** if the flag is ever turned on. Until then the sitemap omits the section entirely, so nothing points at a route that does not exist |
| 2026-09-06 | Step 20 | **The sitemap's products section walks the catalogue variant by variant on every generation**, bounded by `MaxWalkedVariants` and served behind a fifteen-minute output cache | ⏳ Accepted for now. There is no seam that counts or pages products by slug, because `IProductProjectionSource` walks variants — which is what a read-model needs. If it becomes a problem the honest fix is to read the search projection instead, and it is contained to `SitemapBuilder` |
| 2026-09-06 | Step 20 | **The HTML sanitiser is a regular-expression pass over a tag stream, not an HTML5 parser** | ⏳ A deliberate trade, recorded rather than hidden: a parser is the stronger answer and a dependency this solution does not have. The pass is conservative in the direction that matters — anything it cannot confidently recognise as an allowed tag is removed. **Step 29 owns an adversarial corpus** (`TEST_DEBT.md`), and if it fails, an allow-list parser is the fix |
| 2026-09-06 | Step 20 | **The custom-HTML block is stored unsanitised, on purpose** | ℹ️ Sanitising it would make it useless and leave nothing that could carry an embed, which is the reason it exists. It is guarded by a permission **and** a feature flag, the flag is re-checked at render time so switching it off is a remedy, and the `merchandiser` role bundle deliberately does not hold the permission. ADR-020 |
| 2026-09-06 | Step 20 | **A new block type is a release**, not an afternoon: `BlockCatalog` and an Angular component have to ship together | ℹ️ Inherent to the promise. The schema is served over `GET /admin/pages/block-types` rather than compiled into the admin app, so at least the editor's form and the validator that judges it cannot drift. ADR-020 |
| 2026-09-06 | Step 20 | **Rule membership is eventually consistent, and the event path appends rather than re-sorting** | ℹ️ Deliberate. Re-sorting on an event means re-evaluating the whole collection, which is the expensive path the event path exists to avoid; the sweep restores the order within thirty minutes. A product at the end of a carousel for half an hour is a better outcome than one that does not appear at all. The admin screen should say so (Step 28) |
| 2026-09-06 | Step 20 | **A page's `Archived` state and its soft delete are two different things**, and the API offers both | ℹ️ Archiving is the ordinary end of a page's life and leaves the row in the admin history; deletion hides it everywhere and is **refused once a page has ever been published**. The rule is not squeamishness — a published page has links pointing at it, and the supported way to retire one is to archive it and add a redirect |
| 2026-09-06 | Step 20 | **The preview is an admin endpoint, not a token on the store surface** | ℹ️ A security decision. A preview token is a URL that grants access to unpublished content and gets pasted into chat threads; this needs the caller's own bearer token and their own permission on every request. The storefront's server-side renderer holds one when an editor asks to see a draft (Step 23) |
| 2026-09-06 | Step 20 | **`GET /store/content/banners` varies by whether the visitor is signed in**, which is the only non-anonymous read on the storefront content surface | ⏳ Cached with the short public policy rather than the long one. **Step 23 must not cache it further per user**, and Step 32's edge configuration must not treat it as fully public. Flagged because it is the one place a cache misconfiguration would show one visitor another's banner |
| 2026-09-06 | Step 20 | **The redirect resolver writes on a `GET`**, incrementing a hit counter | ℹ️ Deliberate and switchable (`Content:TrackRedirectHits`). One `UPDATE` on a row already in the buffer cache, on a path that only runs for a URL that would otherwise have been a 404, and the counter is allowed to lose a write. Without it a merchandiser has no way to tell which of four hundred rules still matter |
| 2026-09-06 | Step 20 | **A page's `lastmod` is `content_changed_at`, a column separate from `updated_at`** | ℹ️ Necessary rather than tidy. A crawler told that every page changed because somebody renamed one menu learns to ignore `lastmod` entirely, and then it is worth nothing on the day a page really does change |
| 2026-09-06 | Step 20 | **A bug was found and fixed while unit-testing path normalisation**: `Uri.TryCreate` reads a value beginning with two slashes as a UNC path on Windows, so `//sale//summer//` normalised to `/summer` | ✅ **RESOLVED** in the step. The absolute-URL branch is now gated on an explicit `http`/`https` scheme. Recorded because it is exactly the class of defect the sprint's rule 1 exists to catch, and it would have stayed invisible until a redirect silently stopped matching |
| 2026-09-06 | Step 20 | **`PageBlockBinder` re-validates a version snapshot on rollback** | ℹ️ Deliberate. A snapshot is content that was valid when it was written, and a build in which a block type has since gained a required field would otherwise restore something the current storefront cannot render. Refusing with the field named is a problem an editor can solve; a rendered gap is one they report as a bug |
| 2026-09-06 | Step 20 | **Stock is deliberately not subscribed to.** A collection includes out-of-stock products by default | ℹ️ It is the subscription somebody will try to add. Subscribing to the highest-volume event in the platform in order to remove and re-add rows nobody wanted removed would be the most expensive way to make a curated page worse. The rule has an `includeOutOfStock` switch for the store that disagrees |
| 2026-09-06 | Step 20 | **`Reviews.ReviewPublished` is not consumed here either.** A product card's rating is copied from the catalogue, which has no reviews until Step 21 | ⏳ Expected, and the same gap Step 19 recorded. Cards and the `AggregateRating` in structured data are wired and empty until **Step 21** publishes the event and both modules subscribe |
| 2026-09-06 | Step 20 | **The eight compiler warnings in `KlaraHome.Modules.Payments` are still open** | ⏳ Unchanged from Step 19 and still not this step's to fix (protocol rule 6). **Step 28A** |
| 2026-09-06 | Step 21 | **Two new shared seams were added to `KlaraHome.Contracts`: `IOrderPurchases` (Orders) and `IInventoryAgeing` (Inventory)** | ℹ️ Intentional, and each is implemented by the module that owns the data. `IOrderPurchases` is the whole of the step's acceptance criterion — one definition of *delivered*, in the module that owns the state machine deciding it — and is the narrowest of ordering's five outward seams: it answers one yes-or-no question about the past and cannot write. `IInventoryAgeing` exists because ageing is the one inventory number no event carries |
| 2026-09-06 | Step 21 | **Three integration events were added — `ReviewPublished`, `ProductRatingChanged`, `VendorRatingChanged` — where `02-domain-model.md` §6 named one** | ℹ️ A review being published and an average moving are different facts with different causes: the average also moves when an approved review is refused, when a reviewer edits their score, and when an upheld complaint takes one down. A consumer subscribed to the publication would show a stale average for the other three. §6 corrected and recorded in `CHANGE_LOG.md` |
| 2026-09-06 | Step 21 | **`Catalog`, `Vendors` and `Search` each gained an event handler this step wrote** | ℹ️ The designed integration rather than scope creep: `Product.ProjectRating` and `Vendor.RecordRating` have existed since Steps 10 and 9 with no caller, and §6 has always named Catalog and Search as the consumers. This is what finally makes `ProductProjection.RatingAverage` something other than null — the gap Steps 19 and 20 both recorded |
| 2026-09-06 | Step 21 | **`03-database-design.md` §4.18 described seven materialised views, and none was built** | ✅ **RESOLVED** — §4.18 rewritten and **ADR-021** raised. A materialised view named `reporting.mv_daily_sales` has to select from `orders`, `payments` and `catalog`, which §2.1 forbids outright — and being declared in DDL puts it out of reach of the architecture tests, which makes it worse than the violation the rule was written to prevent. The module keeps its own facts instead |
| 2026-09-06 | Step 21 | **There is no back-fill for the reporting facts.** Reporting begins the day the module is deployed with the ingest on | ⏳ Inherent to the design and named in ADR-021. It makes `reporting.fact-ingest` the one feature flag in the platform that is **not** safe to leave off: a period with it disabled is a permanent hole in every report over it. **Step 32 must not ship with it off**, and a store migrating from another platform starts these reports empty |
| 2026-09-06 | Step 21 | **Commission on the daily take-rate cut is apportioned across a cycle's lines by value, not exact** | ⏳ `SettlementCycleClosed` carries one commission figure per cycle; the per-line rate is frozen in `orders`, which this module may not read. Exact for a flat commission plan, approximate for a per-category one. The cycle-level figure in the settlement summary is always exact, and only the daily cut is an estimate. Named in the code, in ADR-021 and in `TEST_DEBT.md` |
| 2026-09-06 | Step 21 | **The conversion funnel starts at the basket.** There is no "viewed a product" step | ℹ️ Deliberate. It needs a client-side analytics pipeline this platform does not have, and inventing a number for it would be worse than admitting where the measurable journey begins. Revisit if Step 31 adds front-end telemetry |
| 2026-09-06 | Step 21 | **`Inventory.StockLevelChanged` is deliberately not subscribed to by Reporting** | ℹ️ It is the subscription somebody will try to add. It is the highest-volume message on the platform and would tell the module nothing it can report on: a balance is a state, and every stock question in the thirteen reports is either about a movement — which arrives as a sale or a return — or about age, which the daily snapshot answers |
| 2026-09-06 | Step 21 | **CSV exports are stored under `reporting/exports/` in the private bucket rather than registered in `media.files`** | ℹ️ A departure from how every other generated document on this platform is stored, and a deliberate one. The media library identifies uploads by sniffing their magic number (`FileInspector`), which is what stops the store serving executable content from its own domain; CSV has none. A report is served by a signed URL minted per request instead. ADR-021 |
| 2026-09-06 | Step 21 | **Nothing sweeps expired report exports out of object storage.** `Reporting:ExportRetentionDays` is declared and read by nothing yet | ⏳ **Step 31** owns retention and the orphan sweep, and this is the same shape as the media orphan sweep already parked there. A stored export is a copy of commercial data that can always be produced again from the facts, so keeping them for ever is a growing liability for no benefit |
| 2026-09-06 | Step 21 | **Moderation is a configuration value (`Reviews:AutoApprove*`), not a store setting** | ℹ️ Deliberate, and the one policy-shaped value in this module that is not in the settings store. Publishing user content unreviewed puts an intermediary in a different position under the IT Rules (`07-security-compliance.md` §5); that is not a switch a merchandiser should be able to flip from an admin screen at four on a Friday. All three ship **off** |
| 2026-09-06 | Step 21 | **A review's author name is shortened at write time ("Priya S.") rather than at read time** | ℹ️ A review is a public document (`07-security-compliance.md` §4) and this module has no use for the full name. Shortening on read would leave it sitting in the schema one bad projection away from a product page. The initial is taken from the **last** part of the name, because "Priya Ramesh Kumar" is called Priya Kumar |
| 2026-09-06 | Step 21 | **An anonymous stock subscription stores an email address**, which is the only personal data this module collects from somebody with no account | ⏳ Necessary — an alert with nowhere to send it is a row nobody will ever read — and bounded: the subscription expires after ninety days and the row is then closed. **Step 31's retention sweep must delete the address**, and Step 29 should confirm an expired row cannot be used to send anything |
| 2026-09-06 | Step 21 | **Both stock alerts are Marketing rather than transactional in the notification model** | ℹ️ Load-bearing rather than a technicality. Nobody ordered anything; somebody expressed interest, and treating interest as consent to be messaged is exactly what the preference centre exists to prevent. A recipient who has opted out of Marketing gets a suppressed row rather than a message |
| 2026-09-06 | Step 21 | **Stock alerts are queued outside the transaction that closes the subscriptions they fired** | ℹ️ `INotifier` writes in its own scope by design (Step 8), and holding a transaction open across a fan-out to five hundred recipients would be worse than the failure it prevents. The inbox row and the closures are in one `SaveChanges`, so the worst case is an alert about a restock that did not commit — a shopper visiting a page and finding nothing |
| 2026-09-06 | Step 21 | **The abuse-report uniqueness rule applies only to signed-in reporters.** An anonymous one has nobody to be unique against | ⏳ Held by the rate limiter on the endpoint instead. The trade is deliberate: a store that required an account to report unlawful content would be a store that made itself hard to tell. **Step 29 should confirm the limiter actually holds** under a scripted report flood |
| 2026-09-06 | Step 21 | **`GET /admin/reports/{key}?format=csv` produces a run and returns the run, rather than streaming the file** | ℹ️ Deliberate indirection. A report somebody clicked for and one that arrives by email every Monday are then the same artefact, produced by the same code and recorded in the same log. The caller downloads it through `/admin/report-runs/{id}/download`, which mints a fifteen-minute signed link |
| 2026-09-06 | Step 21 | **A schedule's report key is not editable**, only its timetable and recipients | ℹ️ Its run history points at it. Changing the report underneath would leave a log in which one schedule produced two different things with no way to tell from a row which. Delete it and make the other one |
| 2026-09-06 | Step 21 | **A failed scheduled report still advances its `next_run_at`** | ℹ️ The alternative turns a report that fails for a structural reason into a job that retries every five minutes for ever and fills the run log with the same error. The failure is on the run row where an operator can see it, and the next scheduled occurrence tries again. **Step 31 should alert on a run in `Failed`** |
| 2026-09-06 | Step 21 | **The inventory snapshot saves per page, so a walk that fails two thirds of the way through leaves two thirds of a snapshot** | ⏳ And the existence check then skips the day, because re-running it would be the doubling the unique index exists to prevent — and a doubled day makes a trend line wrong on both sides of it. A partial snapshot shows in the report as a smaller number. **Step 31 should alert when a day's snapshot is materially smaller than the day before** |
| 2026-09-06 | Step 21 | **The eight compiler warnings in `KlaraHome.Modules.Payments` are still open** | ⏳ Unchanged since Step 19 and still not this step's to fix (protocol rule 6). They are the only warnings in a clean solution rebuild. **Step 28A** |
| 2026-09-06 | Step 21 | **The README has no section for the Content module (Step 20).** Every backend step from 15 to 19 has one, and Step 21 added its own two | ⏳ Documentation debt rather than a gap in the work — Step 20's card and `CHANGE_LOG.md` row are complete. Backfill at **Step 28A**, when the whole README is read against a system that boots |
| 2026-09-06 | Step 21 | **`ModuleBoundaryTests.A_module_exposes_nothing_publicly_except_its_module_class` has been RED since Step 20 landed.** `KlaraHome.Modules.Content` exposes 41 public `Application` records; the rule is that a module's DTOs stay internal and only `KlaraHome.Contracts` crosses the boundary. Confirmed pre-existing against `git show HEAD` — Step 20's card claimed a clean compile but never claimed this suite green | ⏳ **Not fixed inline** (protocol rule 6). Reviews and Reporting were written with the same mistake and **were corrected within this step**, so the suite now names only Content. The fix is one pass over `ContentContracts.cs` and `AdminContentEndpoints.cs`; it is behaviourally inert because nothing outside the assembly references those types. **Raised at this boundary for a decision: fix it now, or leave it to Step 28A** — but it should not sit red for six more steps, because a red gate stops being read |
