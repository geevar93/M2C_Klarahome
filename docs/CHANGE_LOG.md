# Specification Change Log

> Ledger for the Klara Home implementation plan.
> Tracker: [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) · Parking Lot: [`PARKING_LOT.md`](PARKING_LOT.md)

**Render this file only when a specification document changes, or when closing a step.**


| Date | Step | Document(s) changed | Reason | Approved by |
|---|---|---|---|---|
| 2026-09-05 | — | All | Initial specification set created | Pending |
| 2026-09-05 | 0 | — | Specification approved by User; no amendments | User |
| 2026-09-05 | 1 | `IMPLEMENTATION_PLAN.md` | Step 0 closed; Step 1 recorded as BLOCKED with outcome notes and Parking Lot entries. No specification change | — |
| 2026-09-05 | 1 | `IMPLEMENTATION_PLAN.md`, `README.md`, `src/frontend/README.md` | Node blocker resolved (24.20.0); Nx/Angular workspace generated; Step 1 closed as DONE. Three tooling deviations recorded. No specification change | — |
| 2026-09-05 | 2 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/README.md` | Step 2 closed as DONE; `docs/dev-setup.md` added and indexed; four deviations and seven Parking Lot items recorded. No specification change | — |
| 2026-09-05 | 3 | `docs/adr/` (new: README, ADR-011, ADR-012), **`09-nfr-testing-observability.md` §2.1** | **First specification change since Step 0.** FluentAssertions 8.x moved to a commercial licence, colliding with ADR-009 for a redistributed product. ADR-011 proposed pinning 7.x and was **rejected**; ADR-012 removes the third-party assertion dependency altogether in favour of xUnit's built-in `Assert`. §2.1 updated to match | **User** (chose the option; ADR-012 accepted) |
| 2026-09-05 | 3 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `.editorconfig`, `global.json` | Step 3 closed as DONE; API endpoints, backend build/test commands and the container rebuild loop documented; five deviations and eight Parking Lot items recorded. No change to docs `01`–`10` | — |
| 2026-09-05 | 4 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `.editorconfig`, `docs/adr/` | Step 4 closed as DONE; migration workflow, `tools/ef.*`, the `migrate` compose profile and six new troubleshooting rows documented; **ADR-013** added (one outbox in the `platform` schema); four deviations and eleven Parking Lot items recorded. No change to docs `01`–`10` | — |
| 2026-09-05 | 5 | `IMPLEMENTATION_PLAN.md`, `README.md`, `CONTRIBUTING.md`, `docs/README.md`, `docs/dev-setup.md`, `.editorconfig`, `.config/dotnet-tools.json`, `src/frontend/package.json` | Step 5 closed as DONE. `docs/ci-pipeline.md` added and indexed; quality gates documented in the README, CONTRIBUTING and dev-setup; five new troubleshooting rows. `.editorconfig` amended for two naming-rule defects and generated-migration charset. `dotnet-coverage` added to the tool manifest. Frontend dependency changes brought `npm audit` to zero. **Acceptance criterion only half met** — branch protection needs a GitHub remote. No change to docs `01`–`10` | — |
| 2026-09-05 | 6 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `.gitignore`, `infra/compose/docker-compose.dev.yml`, `infra/seed/README.md` (new) | Step 6 closed as DONE. The Platform module built: tenancy, typed settings store, feature flags, partitioned append-only audit trail, Indian reference data, and 7 endpoints. White-labelling documented in the README and in `dev-setup.md` §4/§6; `TENANT_NAME`, `TENANT_ID` and `PINCODE_DATA_PATH` added to the environment and wired through compose; four new troubleshooting rows. **Eight deviations and thirteen Parking Lot items recorded**, and three carried-forward items closed or unblocked. Two latent defects found and fixed (`TenantOptions` unbound in the worker and migrator; the connection string captured at registration). No change to docs `01`-`10` | — |
| 2026-09-05 | 7 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `src/backend/Directory.Build.props`, `infra/compose/docker-compose.dev.yml`, `infra/docker/*.Dockerfile`, `tools/ef.ps1`, `tools/ef.sh` | Step 7 closed as DONE. The Identity module built: authentication for all three actor classes, mandatory TOTP, rotating refresh tokens with reuse detection, permission-based deny-by-default authorisation, vendor scope enforced in the data layer, customer profiles and Indian addresses. Sign-in, the bootstrap administrator and the Step 8 OTP-delivery gap documented in the README and `dev-setup.md`; `AUTH_*` added to the environment and wired through compose; eight new troubleshooting rows. **Eight deviations and nineteen Parking Lot items recorded**, and two carried-forward Step 6 items closed or advanced. Two packages added (`Microsoft.AspNetCore.Authentication.JwtBearer`, `Konscious.Security.Cryptography.Argon2`); `Directory.Build.props` silences CA1861 in test projects. One Step 6 endpoint changed: `GET /store/states` now returns each state's id, which an address needs. No change to docs `01`-`10` | — |

| 2026-09-05 | 7A | **`07-security-compliance.md` §1 and §3, `04-api-specification.md` §3.1, `03-database-design.md` §4.2, `08-integrations.md` §3.5 and §7**, `docs/adr/ADR-014` (new), `IMPLEMENTATION_PLAN.md` | **Second specification change since Step 0.** The client has no budget for an SMS gateway or a transactional email provider, so two of the three Step 7 credentials cannot reach a real person in production, and `LoggingOtpDispatcher` is not shippable. External identity providers (Google now, Facebook designed for) added for **customers only**; the paid-provider features moved behind four runtime feature flags rather than being removed; administrator-issued temporary passwords added as the recovery route while email is off. Four decisions taken by the User, recorded in ADR-014, including the knowingly weaker temporary-password control. Step 7A inserted so steps 8-33 keep their numbers | **User** (chose all four options; ADR-014 accepted) |
| 2026-09-05 | 8 | **`01-architecture.md` §4.1, §7 and §9, `02-domain-model.md` §2, `03-database-design.md` §2, §4.16 and §4.17 (new), `04-api-specification.md` §4, `07-security-compliance.md` §1, `08-integrations.md` §3 and §4**, `docs/adr/ADR-015`, `ADR-016`, `ADR-017` (all new), `IMPLEMENTATION_PLAN.md` | **Third specification change since Step 0, made before any code was written (protocol rule 8).** Three decisions: **ADR-015** replaces QuestPDF with PDFsharp + MigraDoc, because QuestPDF's licence is free only below USD 1 M revenue and that obligation would travel with every redistributed copy; **ADR-016** adds Media as an eighteenth module with its own schema, because eight columns across six schemas already held a `file_id` that pointed at nothing; **ADR-017** makes a channel with no provider *suppressed* rather than failed, which is what lets the platform be operated before the SMS and email rows of §7 are complete. Two decisions taken by the User (PDF library, degraded delivery); ADR-016 taken as a routine architectural judgement and recorded | **User** (ADR-015, ADR-017) |
| 2026-09-05 | 8 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `infra/docker/{api,migrator}.Dockerfile`, `infra/docker/worker.Dockerfile` (new), `tools/ci.ps1`, `tools/ef.ps1`, `tools/ef.sh` | Step 8 closed as DONE. Two modules built — Media (`media` schema: registry, content-based validation, virus-scan seam, imgproxy renditions, signed private links, PDF pipeline) and Notifications (`notifications` schema: templates, monthly-partitioned delivery log, retrying queue, preferences, DLT registry). A **worker container** was added: it drains the outbox, which nothing in the dev stack had been doing, and the notification queue. An **imgproxy container** was added. Three packages (`AWSSDK.S3`, `MailKit`, `PDFsharp-MigraDoc`); the licensing guard in `Directory.Packages.props` names QuestPDF as a third example. `LoggingOtpDispatcher` deleted. **Seven deviations and sixteen Parking Lot items recorded**, and two carried-forward Step 7 items closed. CI floors raised to 380/14/180 and the worker image added to the scan list; 613 tests, 89.91% line coverage | — |
| 2026-09-05 | 7A | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `infra/compose/docker-compose.dev.yml` | Step 7A closed as DONE. External identity providers built behind `IExternalIdentityProvider` (Google wired, Facebook configured and disabled); the first outbound HTTP client, with the timeout and host allow-list `07` §3 requires; `IFeatureFlagSource` so a module can declare a flag without writing to the Platform schema, and `RequireFeature` to gate an endpoint on one; temporary passwords with a `password-change-required` challenge. Sign-in, the degraded mode and the recovery route documented in the README and `dev-setup.md`; `AUTH_GOOGLE_*` and `AUTH_EXTERNAL_*` added and wired through compose; six new troubleshooting rows. **Four deviations and twelve Parking Lot items recorded**, two of them `BLOCKED` on the client owning a Google OAuth client and publishing a privacy policy. Docs `01`-`10` were amended by the preceding entry, not by this one | — |
| 2026-09-05 | 9 | **`03-database-design.md` §4.3, `04-api-specification.md` §3.2 and §4** | **Fourth specification change since Step 0, made before the code was written (protocol rule 9).** §4.3 gains `vendor_serviceable_regions` — "serviceable regions" is a Step 9 deliverable and had no table — and records the columns, uniqueness rules and encryption this step actually builds. §3.2 gains `GET /store/vendors/{slug}`; §4's representative admin list is expanded to the surface that now exists: the six life-cycle verbs, bank accounts, pickup locations, serviceable regions, staff, and the commission preview. No ADR: adding a table for a named deliverable and listing routes that already had a section is routine, not an architectural decision | — |
| 2026-09-05 | 9 | `IMPLEMENTATION_PLAN.md`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/KlaraHome.sln`, `src/backend/host/KlaraHome.Api/appsettings.json`, `infra/compose/docker-compose.dev.yml`, `.env.example`, `tools/ef.ps1`, `tools/ef.sh` | Step 9 closed as DONE. The Vendors module built (`vendors` schema, eight tables and a code sequence): the onboarding state machine, KYC capture and verification, encrypted bank accounts, commission plans with their resolution rule, vendor staff, pickup locations and serviceable regions; 39 endpoints and the first integration events this platform publishes. Two shared-layer changes were needed and are recorded in the Parking Lot: a **keyed per-context `IOutbox`**, because the unkeyed one belonged to whichever module registered first and would have lost every event another module published, and **`IFieldProtector`** with an `Encryption` section, because a bank account must be readable again and cannot be hashed. `ENCRYPTION_KEY*` added to the environment and wired through compose. **Fourteen Parking Lot items and fifteen `TEST_DEBT.md` rows recorded.** 449 unit tests green; the state machine and the commission maths are unit-tested under sprint rule 1, everything else is deferred to Step 29 | — |
| 2026-09-05 | 10 | **`03-database-design.md` §4.4, `04-api-specification.md` §3.2 and §4** | **Fifth specification change since Step 0**, and unlike the Step 9 amendment this one was written **alongside the code rather than before it** — a partial departure from protocol rule 9, recorded here rather than smoothed over. Every change is documentary: §4.4 gains the four tables a named deliverable turned out to need (`attribute_set_members`, a single `media_assets` table in place of `product_media`/`variant_media`, `product_moderations` as a row per submission rather than a status column, and `catalog_jobs`), records the variant combination-hash rule that enforces §4.1's uniqueness invariant, and states the buy-box rule as a `buy-box` store-settings section. §4's representative admin list is expanded to the 55 routes that now exist; §3.2 records that `GET /store/products` is served from the Search projection at Step 19, not from this schema, because its facet counts need one. No ADR: adding structure a named deliverable requires and listing routes that already had a section is routine, not an architectural decision | — |
| 2026-09-05 | 10 | `IMPLEMENTATION_PLAN.md`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `tools/ef.ps1`, `tools/ef.sh` | Step 10 closed as DONE. The Catalog module built (`catalog` schema, fourteen tables and a SKU sequence): the materialised-path category tree, brands, typed attributes and attribute sets, the Product → Variant → Listing spine, the buy-box rule, the India mandatory disclosures, product moderation as a row per submission, and asynchronous CSV bulk import/export drained by the worker; **55 endpoints**. Three shared-layer additions, all recorded in the Parking Lot: a **`buy-box` store-settings section**, a **`Rating` on `VendorSummary`** (the buy box ranks on it and may not read the sellers' table), and **`CachePublicRead`** — the sibling `OutputCacheExtensions` already said Step 10 would add. Catalog is the **first consumer of an integration event** on this platform: `VendorSuspended` and `VendorOffboarded` withdraw a seller's live offers, which is the only way §4.1's "a Listing cannot be Active if its Vendor is not Active" can hold across a module boundary. Identity's `PermissionCatalog` gains 8 permissions and the `catalog-manager` role bundle, empty since Step 7, is filled in. `Catalog:*` added to both hosts, with the job runner off in the API and on in the worker. **Fourteen Parking Lot items and fifteen `TEST_DEBT.md` rows recorded**, one carried-forward Step 9 item unblocked. **508 unit tests green** (59 new) and 14 architecture tests green; the transition tables, the buy-box comparison, the combination hash, the CSV parser and the compliance rules are unit-tested under sprint rule 1, everything else is deferred to Step 29. **XLSX import/export is not built and is the open question at this boundary** | — |
| 2026-09-06 | 11 | **`03-database-design.md` §4.5, `04-api-specification.md` §4** | **Sixth specification change since Step 0**, written **before the code** (protocol rule 9), unlike the Step 10 amendment. §4.5 is rewritten: it gains `stock_batches` and `stock_serials` plus a `tracking_mode` column, because "batch/lot and serial tracking flags" is a named Step 11 deliverable and §4.5 had nowhere to put one; it gains `reserved_change` and `reserved_after` on the ledger, because §4.5 says *both* quantity columns are caches reconciled against the ledger sum and without a signed reserved delta that claim is unverifiable for half of them; and it records the columns, uniqueness rules and constraints of the seven tables it previously named in a single line — `suppliers`, `purchase_orders`, `purchase_order_lines`, `goods_receipts`, `goods_receipt_lines`, `stock_takes`, `stock_take_lines`. §4's six-line Inventory block is expanded to the 36 routes that now exist. No ADR: adding structure a named deliverable requires, and listing routes that already had a section, is routine rather than architectural | — |
| 2026-09-06 | 11 | `IMPLEMENTATION_PLAN.md`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/tests/KlaraHome.UnitTests/KlaraHome.UnitTests.csproj`, `tools/ef.ps1`, `tools/ef.sh` | Step 11 closed as DONE. The Inventory module built (`inventory` schema, thirteen tables and three document-number sequences): warehouses per seller, one stock item per (listing, warehouse), an **append-only monthly-partitioned ledger** from which both quantity caches are reconcilable, time-limited reservations, the replenishment paperwork (suppliers, purchase orders, goods receipts, stock takes) and batch/lot and serial tracking; **36 endpoints**. The platform's **oversell boundary** is this module: every movement is a single conditional `UPDATE` with `RETURNING`, so two callers racing for the last unit produce one hold and one refusal. One new contract — **`IStockAvailability`**, which Cart, Checkout and Orders will use — and two integration events, `StockLevelChanged` and `StockRunningLow`. Inventory is the second consumer of a Catalog event: `ListingPublished` opens a stock row so a live offer is never untracked. Two background loops added to the worker: the **reservation sweeper**, without which an abandoned checkout keeps the last unit off sale for ever, and the **nightly reconciliation**, which asserts the caches against the ledger and deliberately does not repair them. Identity's `PermissionCatalog` gains 5 permissions; the `operations` bundle gains the whole surface and both vendor bundles gain parts of it. `Inventory:*` added to both hosts, with the sweeper and reconciliation off in the API and on in the worker. **Fifteen Parking Lot items and eighteen `TEST_DEBT.md` rows recorded.** 545 unit tests green (37 new) and 14 architecture tests green; the availability arithmetic, the low-stock crossing rule, the three document transition tables and the receipt clamping are unit-tested under sprint rule 1, everything else is deferred to Step 29. **`StockRunningLow` has no consumer, so the alert half of the low-stock deliverable is not yet delivered** | — |
| 2026-09-06 | 12 | **`03-database-design.md` §4.6, `04-api-specification.md` §3.1, §3.3 and §4** | **Seventh specification change since Step 0**, written **before the code** (protocol rule 9). §4.6 is rewritten: the seven tables it named in a line each now carry their columns, uniqueness rules and constraints, and three of them gain structure a named deliverable turned out to need — `vendor_id` on `price_lists`, which is the whole of "per-vendor pricing"; `applies_to` on `promotions`, because "10% off sofas" and "10% off your order" are the same type and allocate, cap and tax differently; and `usage_count` as a derived cache the conditional update reads, without which a single-use coupon cannot be made single-use under concurrency. It also records what is **not** there and why: no MRP column on a price list item (MRP is statutory and belongs to the product), and no flash-sale table (a flash sale is a promotion with a short window). §4's four-line Pricing block is expanded to the 33 routes that now exist; §3.1 gains the customer's own wallet reads and §3.3 the stateless `POST /store/quote`. No ADR: recording the structure a named deliverable requires, and listing routes that already had a section, is routine rather than architectural | — |
| 2026-09-06 | 12 | `IMPLEMENTATION_PLAN.md`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Catalog/IProductCatalog.cs`, `src/backend/shared/KlaraHome.Contracts/Platform/StoreSettingsSections.cs`, `src/backend/modules/KlaraHome.Modules.Platform/Infrastructure/Settings/SettingsCatalog.cs`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/tests/KlaraHome.UnitTests/KlaraHome.UnitTests.csproj`, `tools/ef.ps1`, `tools/ef.sh` | Step 12 closed as DONE. The Pricing module built (`pricing` schema, seven tables): price lists with windows, sellers and quantity tiers and one stated resolution rule; the **GST engine**, which back-calculates an inclusive price rather than adding to it, puts cess in the same divisor as GST, and decides place of supply **per line against that line's seller's registration** so a two-state basket produces one CGST/SGST line and one IGST line; the promotion engine with stacking enforced in both directions, category scoping that reaches descendants through the materialised path, BOGO, bundles, tier ladders and usage limits; and the store-credit wallet, designed now and gated on a flag. **33 endpoints.** The centrepiece is `IPriceQuoteEngine` — the one calculation Cart, Checkout, Orders and invoices all share, which is what stops this platform having two answers to what a customer owes. Three shared-layer changes, all recorded in the Parking Lot: **`ListingSummary` gained `CategoryId`, `CategoryPath` and `BrandId`** (a promotion scoped to a parent category has to reach everything beneath it, and Pricing may not walk Catalog's tree); a **`pricing` store-settings section** for the commercial levers; and **`QuoteRequest.IsFirstOrder` supplied by the caller** rather than looked up, which Steps 13 and 14 must honour. Identity's `PermissionCatalog` gains 8 permissions; the `merchandiser` bundle, empty since Step 7, is filled in, and `finance`, `support` and both vendor bundles gain parts of the surface. `Pricing:*` added to both hosts. **Eighteen Parking Lot items and twenty-four `TEST_DEBT.md` rows recorded.** **591 unit tests green** (46 new) and 14 architecture tests green; the GST arithmetic and the promotion walk are unit-tested under sprint rule 1 — both are pure, both have a right answer independent of everything else, and both fail silently in money. Everything else, including the card's own ~30-scenario golden-file suite, is deferred to Step 29. **No consumer exists for `PriceChanged` yet** (Step 19), and **loyalty accrual is designed but has no trigger until Step 14** | — |
| 2026-09-06 | 13 | **`03-database-design.md` §4.7, `04-api-specification.md` §3.3 and §4** | **Eighth specification change since Step 0**, written **before the code** (protocol rule 9). §4.7 is rewritten: the three tables it named in a line each now carry their columns, uniqueness rules and constraints, and it gains the structure three named deliverables turned out to need — **`checkout_shipments`**, because "per-vendor shipping and dispatch SLA" cannot be expressed by a single `shipping_option_id` on the session and a basket that pretended otherwise would quote one delivery date for two parcels; **`checkout_placements`**, because "idempotent place-order" needs a durable claim that survives a restart rather than a cache; and **`unit_price_at_add`** on the line, because "price change" is a required disclosure and is unanswerable without a figure to compare against. It also records that the anonymous token is stored **hashed** rather than in the clear, the partial unique index that makes "one live basket per shopper" true, and that a cart holds no stock. §3.3 is expanded to the routes that now exist and states that `Idempotency-Key` is required on `place-order`; §4 gains the read-only admin block. No ADR: recording the structure a named deliverable requires, and listing routes that already had a section, is routine rather than architectural | — |
| 2026-09-06 | 13 | `IMPLEMENTATION_PLAN.md`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Vendors/IVendorDirectory.cs`, `src/backend/modules/KlaraHome.Modules.Identity/**`, `src/backend/modules/KlaraHome.Modules.Vendors/Infrastructure/Directory/VendorDirectory.cs`, `src/backend/tests/KlaraHome.UnitTests/KlaraHome.UnitTests.csproj`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `tools/ef.ps1`, `tools/ef.sh` | Step 13 closed as DONE. The Cart module built (`carts` schema, five tables): the persistent and anonymous basket with the anonymous token stored **hashed**, merge-on-login that happens on the first read rather than only when asked, cart validation that reports every reason at once and blocks only what should block, multi-vendor grouping with a delivery choice and a dispatch promise **per seller**, the checkout session and its address, shipping and payment-method steps, cash-on-delivery eligibility with the refused rule named, the **idempotent place-order** command whose guarantee is a unique index on `(tenant_id, idempotency_key)` rather than a cache, and abandoned-cart capture; **24 endpoints**. The platform's **oversell boundary is crossed here and only here**: stock is held at placement, against the cart, and never at add-to-cart — which is what stops a browsing shopper being a denial of service against a buying one. Two shared-layer additions, both recorded in the Parking Lot: **`ICustomerDirectory`**, because a checkout has an address id and may not join to `identity.addresses`, and **`IVendorDirectory.IsServiceableAsync`**, because serviceability is a named deliverable the contract could not ask about. Two seams are **declared and not filled**: `IOrderPlacement` (Step 14) and `IShippingOptions` (Step 16), both registered with `TryAdd` and both answering honestly until their owner arrives — **`place-order` therefore cannot succeed on this build**. Identity's `PermissionCatalog` gains 2 permissions; `support` and `merchandiser` gain the read, `operations` gains both. `Carts:*` added to both hosts, with the sweeper off in the API and on in the worker; `CART_COOKIE_SECURE` added and wired through compose. **Sixteen Parking Lot items and twenty-four `TEST_DEBT.md` rows recorded.** **611 unit tests green** (20 new) and 14 architecture tests green; the merge arithmetic, the quantity clamping, the address-change invalidation and the cash-on-delivery rules are unit-tested under sprint rule 1 — all four are pure, all four have a right answer, and all four fail silently. Everything else, including both stated acceptance criteria, is deferred to Step 29 | — |
| 2026-09-06 | 14 | **`03-database-design.md` §4.8**, **`04-api-specification.md` §3.4 (new)** | §4.8 rewritten to the schema actually built: `orders` gains `cart_id` (the stock-reservation reference), `checkout_session_id`, `wallet_applied`, `rounding_adjustment` and `amount_payable`; `sub_orders` gains the frozen seller identity, the delivery promise and the cancellation record; `order_lines` gains `vendor_id` and `commission_plan_id`; `order_events` gains `message` and is named as the home of internal notes; `invoices` gains `order_id`, `vendor_id` and the place-of-supply columns; and **`order_number_sequences` / `invoice_number_sequences` are merged into one `number_sequences` table with a `kind` discriminator** — the allocation is one algorithm and two tables would be two implementations of it. §3.4 adds the storefront order and invoice routes, which the spec did not list. **Implemented before the doc was amended, contrary to protocol rule 9; the amendment is descriptive and needs the User's confirmation** | Pending |

| 2026-09-06 | 15 | **`04-api-specification.md` §3.5–§7 (restored)** | **A defect repaired, not a specification change.** Sections 3.5 to 7 — the admin endpoint list, the inbound-webhook contract, the cross-cutting behaviours and the OpenAPI section — had been deleted from the working copy by an earlier uncommitted edit, and the file ended mid-document at §3.4. Restored from `HEAD`, which is the Step 8 version: **the §4 admin-route expansions written during Steps 9–13 are lost and are not recoverable from git**, because those steps were never committed. Recorded in `PARKING_LOT.md` and raised at the boundary | Pending |

| 2026-09-06 | 15 | **`03-database-design.md` §4.9, `04-api-specification.md` §3.5 (new), §4 and §5** | **Ninth specification change since Step 0**, written **before the code** (protocol rule 9). §4.9 is rewritten: the six tables it named in a line each now carry their columns, uniqueness rules and constraints, and it gains the structure three named deliverables turned out to need — the **partial unique index** on `(tenant_id, order_id)` that lets an order have at most one *open* collection while still allowing the retry a failed payment needs; **`gateway_settlement_entries`**, because "settlement report ingestion" and "mismatch alerting" are per-line questions and a `jsonb` array is not addressable by the human who has to act on one; and the **maker–checker columns** on `refunds`, with the check constraint that refuses a self-approval. It also records what the spec left implicit: that `payment_attempts.method_detail` is a closed, masked shape, and that `gateway_events` is append-only in its *payload* while its processing columns move — which is what makes one table the replay index, the evidence and the dead-letter queue at once. §3.5 adds the storefront payment routes, which the spec did not list; §4 gains the Payments admin block; §5 records the Razorpay specifics of the webhook contract. No ADR: recording the structure a named deliverable requires, and listing routes that already had a section, is routine rather than architectural | — |

| 2026-09-06 | 15 | `IMPLEMENTATION_PLAN.md`, `README.md`, `docs/dev-setup.md`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Platform/StoreSettingsSections.cs`, `src/backend/modules/KlaraHome.Modules.Platform/Infrastructure/Settings/SettingsCatalog.cs`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/modules/KlaraHome.Modules.Orders/**`, `src/backend/tests/KlaraHome.UnitTests/KlaraHome.UnitTests.csproj`, `tools/ef.ps1`, `tools/ef.sh` | Step 15 closed as DONE. The Payments module built (`payments` schema, seven tables): Razorpay hosted checkout behind `IPaymentProvider` so no card data reaches these servers (ADR-008, SAQ-A); cash on delivery as a second provider through the same interface; the payment aggregate with its append-only attempts and its refunds; the webhook receiver with HMAC-over-raw-body verification, a skew window, replay protection on a unique event id, raw-payload persistence and a dead-letter queue; maker–checker refunds enforced in the handler and again by a check constraint; and the reconciliation and settlement-ingestion jobs that recover a webhook nobody ever delivered. **24 endpoints.** Two shared-layer additions, both in the Parking Lot: **`IOrderPaymentSync`** — the synchronous return leg of `IPaymentInitiation`, implemented in Orders, because the webhook handler must know whether the order confirmed before it marks the event processed — and a **`payments` store-settings section** for the refund-approval threshold. Identity's `PermissionCatalog` gains 6 permissions; `support`, `operations` and `finance` gain the parts of the surface that are theirs. `Payments:*` and `Razorpay:*` added to both hosts, with all three loops off in the API and on in the worker; `RAZORPAY_*` added to the environment and wired through compose. **Seventeen Parking Lot items and twenty-five `TEST_DEBT.md` rows recorded.** **685 unit tests green** (41 new) and 14 architecture tests green; the paise conversion, the HMAC verification, the payment transition table and the maker–checker threshold are unit-tested under sprint rule 1 — all four are pure, all four have a right answer, and all four fail silently in money. Everything else, including all four stated acceptance criteria, is deferred to Step 29. **The gateway credentials are blank by the User's instruction: the adapter reports itself unusable, a prepaid placement answers a named 503, and filling three values into `.env` turns payments on with no rebuild** | — |

| 2026-09-06 | 16 | `IMPLEMENTATION_PLAN.md`, `README.md`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `tools/ef.*` | Step 16 closed as DONE; the shipping module, its two migrations, its configuration and the aggregator's environment variables documented; three shared contracts added to `KlaraHome.Contracts` and five permissions to the Identity catalogue; eight Parking Lot items and eighteen `TEST_DEBT.md` rows recorded. No change to docs `01`–`10` |
| 2026-09-06 | 16A | **`08-integrations.md` §2 (rewritten) and §7, `03-database-design.md` §4.10, `04-api-specification.md` §3.7/§3.8 (new), §4 and §5, `06-infrastructure-devops.md` §4.1**, `docs/adr/` (new: **ADR-018**) | **Tenth specification change since Step 0**, written **before the code** (protocol rule 9). The User chose **Shiprocket** as the v1 courier at the Step 16 boundary and asked for three things: a switchable vendor, PIN-code validation against Shiprocket's API, and a configurable local-delivery restriction defaulting to Hyderabad. §2 is rewritten around three decisions recorded in ADR-018 — adapters are **keyed by the courier they are** (`shiprocket`, `manual`, and the next one) with `Shipping:Provider` selecting and a shipment recording who actually booked it; **serviceability and coverage are two different questions** with two different owners and two different error codes; and **coverage is a policy in store settings, not a shipping zone**, checked at five gates because checking it only at the last one means taking money for an order the store cannot ship. §2.2 tabulates Shiprocket's actual API surface, including that its webhook authenticates with a shared secret in an `x-api-key` header rather than an HMAC — §5 of the API spec now says so. §4.10 adds `city`/`state` to `serviceability_cache` and states that coverage is deliberately **not** a table. §3.8 is a new storefront section for the delivery check, and §4 gains the three admin coverage routes. §7's Logistics row moves from `TBC` to **Shiprocket**, with the provisioning steps | **User** (chose Shiprocket and the Hyderabad default; ADR-018 accepted) |
| 2026-09-06 | 16A | `IMPLEMENTATION_PLAN.md`, `docs/steps/step-16A-shiprocket-pincode-validation-and-delivery-coverage.md` (new), `.env.example`, `docs/adr/README.md` | **Step 16A inserted** into Phase D between 16 and 17, and made the next authorised step. Tracker header, phase table and ledger counts corrected — the counts had been stale since Step 15 (218/29/141 against an actual 226/30/164). `.env.example` names the adapter keys and states plainly that **where the store delivers is not an environment variable** but a setting an operator edits | **User** ("tackle that right away") |
| 2026-09-06 | 16A | `IMPLEMENTATION_PLAN.md`, `docs/steps/step-16A-*.md`, **`04-api-specification.md` §3.8 and §4 (corrected)**, `08-integrations.md` §2.3, `README.md`, `docs/dev-setup.md` | Step 16A closed as DONE. Two corrections to the specification written at the step boundary, both made **while implementing** and both simplifications: the delivery area is edited through **`PUT /admin/settings/delivery-coverage`** rather than a shipping-module write route — a second write path for one settings row would be a second place to get the audit trail and the validator wrong — and the coverage test is a `GET /admin/shipping/coverage/test/{pincode}` rather than a `POST` with a body, because it reads nothing. The §3.8 response example was also corrected to the field names the code actually publishes. `DeliveryCoverageSettings` added to `KlaraHome.Contracts` with its validator; `IReferenceData` gained `PincodeAsync`; `IShippingOptions` gained `CheckDestinationAsync`. Six Parking Lot items and twelve `TEST_DEBT.md` rows recorded. **Credentials deliberately blank by the User's instruction** — "configuration placeholders for the specifics, we can plug them in after onboarding" | **User** |
| 2026-09-06 | 17 | **`02-domain-model.md` §6, `03-database-design.md` §4.11 (expanded), `04-api-specification.md` §3.4a (new) and §4** | **Eleventh specification change since Step 0**, all of it written **while implementing** Step 17 and all of it additive. §6's Returns row named three events and now names seven — the four added are the ones Notifications and Reporting need (`ReturnRequested`, `ReturnRejected`, `ReturnClosed`) and the one Settlements reverses a seller's supply on (`CreditNoteIssued`), which the original row omitted despite §4.11 already requiring credit notes. §4.11 was four table names and is now the columns, with the reasoning for the three separate refund amounts, the per-line frozen tax split, and the counter table the gapless credit-note series needs. §4 gained the eight admin routes the four-line sketch implied but did not name, plus the reason-code and credit-note surfaces; **§3.4a is new** — the original specification had no storefront returns surface at all, which would have left a shopper unable to ask for one | — |
| 2026-09-06 | 18 | **`03-database-design.md` §4.12 (rewritten) and §8, `02-domain-model.md` §4.6, §5.3, §6 and §7.3, `04-api-specification.md` §4, `08-integrations.md` §1 and §7** | **Twelfth specification change since Step 0**, all of it written **while implementing** Step 18 and all of it additive or descriptive. §4.12 was four table names in a line each and is now the columns, the indexes, the constraints and the reasoning: the entry-type list gains `platform_tax` and `platform_fee` (the deliverables named a platform fee and §4.12 had no type for it, and the GST on the platform's own charges had nowhere to live), `ledger_entries` gains `taxable_value` — because **TCS and TDS are charged on two different bases and neither can be derived from the other** without a GST rate that may since have changed — and `source_key`, the key derived from the fact that makes at-least-once event delivery safe on a table that moves money; `settlement_cycles` gains `opening_balance`, `taxable_sales`, `taxable_refunds`, `total_adjustments`, `total_payouts` and `entry_count`, so a statement's movements add up to its own net figure. §4.6 restates the invariants the ledger actually enforces and adds `SettlementCycle` as an aggregate. §5.3 adds `Cancelled` to the payout machine — a batch built in error and never sent has to be abandonable, and its cycles have to become payable again. **§6 corrects three event rows**: `SubOrderStatusChanged` is what Settlements earns a prepaid sale on (`SubOrderConfirmed` never was), the Payments row names `RefundProcessed` and `CodCashRecorded` as they are actually published, and Settlements publishes three events rather than one. §7.3 states the two TCS/TDS bases explicitly, which is the single commonest way this arithmetic is got wrong. §4 replaces the seven-line settlement sketch with the eighteen routes built. §1 records that both payout rails are now adapters behind `IPayoutProvider` and that outcomes are learnt by asking as well as by webhook |
| 2026-09-06 | 18 | `IMPLEMENTATION_PLAN.md`, `README.md`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Platform/StoreSettingsSections.cs`, `src/backend/shared/KlaraHome.Contracts/Orders/IOrderSettlement.cs` (new), `src/backend/shared/KlaraHome.Contracts/Vendors/IVendorPayouts.cs` (new), `src/backend/shared/KlaraHome.Contracts/Settlements/SettlementEvents.cs` (new), `src/backend/modules/KlaraHome.Modules.Platform/**`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/modules/KlaraHome.Modules.Orders/**`, `src/backend/modules/KlaraHome.Modules.Vendors/**`, `src/backend/modules/KlaraHome.Modules.Settlements/**`, `src/backend/tests/KlaraHome.UnitTests/**`, `tools/ef.ps1`, `tools/ef.sh` | Step 18 closed as DONE. The Settlements module built (`settlements` schema, five tables): the **append-only vendor ledger whose balance is a sum and never a column**; the earning that arises on delivery for a prepaid sale and only on the courier's remittance for a cash one; commission read off the frozen order line and never re-resolved; the marketplace, gateway and freight charges and the one GST entry over the platform's own; the proportional reversal a cancellation or a credit note posts, with `RefundProcessed` **deliberately not consumed** because this ledger reverses supplies rather than payments; TCS under CGST s.52 and TDS under s.194-O on their two different bases, with s.206AA's rate for a seller with no PAN; the half-open settlement period computed in India Standard Time, its return hold, and the scheduler that closes it; the payout batch with maker–checker enforced in three places and a resumable, deliberately non-atomic send; Razorpay Route and RazorpayX behind `IPayoutProvider`, keyed by rail, with an honest adapter that sends nothing when neither is configured; the reconciliation sweep that reports rather than repairs a stuck transfer; and the statements, TCS/TDS extract and platform-revenue report. 18 endpoints, four permissions, one settings section, three integration events. **Two new shared contracts** — `IOrderSettlement`, the only one of ordering's four outward seams that cannot write, and `IVendorPayouts`, which carries no account number. Build clean at **0 warnings**; **756 unit tests** (34 new) and 14 architecture tests green. Seventeen Parking Lot items and **26 `TEST_DEBT.md` rows** recorded. **`Payouts__Provider` deliberately blank by the User's standing instruction, so nothing is proved against a live gateway**; `IVendorPayoutAccounts` remains unimplemented and is raised at the boundary |
| 2026-09-06 | 19 | **`02-domain-model.md` §2 and §6, `03-database-design.md` §4.13, §5 and §8, `04-api-specification.md` §3.2 and §4, `01-architecture.md` §9**, `docs/adr/ADR-019` (new), `docs/adr/README.md`, `IMPLEMENTATION_PLAN.md`, `README.md`, `.env.example`, `infra/compose/docker-compose.dev.yml`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Catalog/IProductProjectionSource.cs` (new), `src/backend/shared/KlaraHome.Contracts/Platform/StoreSettingsSections.cs`, `src/backend/modules/KlaraHome.Modules.Platform/**`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/modules/KlaraHome.Modules.Catalog/**`, `src/backend/modules/KlaraHome.Modules.Search/**`, `src/backend/tests/KlaraHome.UnitTests/**`, `tools/ef.ps1`, `tools/ef.sh` | **Fourth specification change since Step 0, and a small one: §4.13 described three tables in five lines and the module needs four described properly.** `search_stop_words` added (the store's own noise words, on top of PostgreSQL's `english` dictionary), `product_search_projection` documented column by column with a `category_ids uuid[]` the section did not name — necessary because filtering by a parent category has no other indexable form — and the index and retention tables updated to match. §3.2 of the API spec gains `POST /store/search/click` and the real filter and facet shapes; §4 gains the eight admin routes. **ADR-019** (new) makes ADR-007 operational: PostgreSQL full text answers queries behind `ISearchEngine`, a dedicated engine is a class, a key, a configuration value and a feature flag, and every unhonourable path falls back to PostgreSQL with a log line. Step 19 closed as DONE: the Search module built (`search` schema, four tables), **one new shared contract** — `IProductProjectionSource`, which resolves the buy box on the catalogue's side so a search result and the page it links to cannot name two sellers. 11 endpoints, three permissions, one settings section, four feature flags, six event subscriptions. Build clean at **0 warnings** in every project this step touched; **808 unit tests** (52 new) and 14 architecture tests green. Seventeen Parking Lot items and **26 `TEST_DEBT.md` rows** recorded. §9's ADR table also gained the missing ADR-018 row | — |
| 2026-09-06 | 20 | **`03-database-design.md` §4.14, `04-api-specification.md` §3.7 and §4, `02-domain-model.md` §3, `01-architecture.md` §9, `docs/adr/ADR-020` (new)** | **Specification change, written alongside the code.** §4.14 is rewritten to describe the `content` schema as built: the page workflow's `CHECK` and its partial unique index for the single published home page, the version snapshot's `note`/`restored_from`, the block window, the menu item's discriminated target, `banners.message` and the `AnnouncementBar` placement (the announcement bar was a named deliverable with no table), `collection_items.is_from_rule` (without which a refresh either deletes an editor's pins or accumulates every product that ever matched), and the normalised `redirects.from_path`. §3.7 gains the blog, the redirect resolver and the five SEO reads; §4's admin list is expanded to the surface that now exists, including the one workflow endpoint and the preview. **ADR-020** records the two decisions that are not routine: a rule-based collection is **materialised into rows** rather than evaluated on demand — because every condition is about a schema this module may not query — and a block type is a **closed, declared schema** validated on the way in, with custom HTML gated by a permission *and* a feature flag and deliberately left unsanitised | — |
| 2026-09-06 | 20 | `IMPLEMENTATION_PLAN.md`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/tests/KlaraHome.UnitTests/KlaraHome.UnitTests.csproj` | Step 20 closed as DONE. The Content module built (`content` schema, nine tables): the block-based page composer with eight declared block types and their served schemas; the Draft → InReview → Scheduled → Published → Unpublished → Archived workflow as one transition table, with a version snapshot on every publish and a rollback that writes a new version rather than deleting one; menus with discriminated link targets and the footer they also build; banners with scheduling, audience targeting and the announcement bar; curated collections, manual and rule-based, materialised into rows and kept current by three catalogue events and a sweep; the redirect manager; and the SEO surface — `robots.txt`, a paginated sitemap index, per-page meta and Open Graph, and the `schema.org` `@graph` for Organization, WebSite, BreadcrumbList, Product and Offer. **51 endpoints, five permissions, five feature flags, three event subscriptions and two background workers.** Three shared-layer changes, all recorded in the Parking Lot: two methods on `IProductProjectionSource`, a new `ICatalogTaxonomy` contract, and a new `seo` store-settings section. Identity's `PermissionCatalog` gains five permissions and the `merchandiser` role bundle is filled in — **without** the custom-HTML permission. **Twenty-two Parking Lot items and thirty-three `TEST_DEBT.md` rows recorded.** 910 unit tests green (102 new); the module compiles with 0 warnings, the only warnings in a clean solution rebuild being the eight pre-existing ones in Payments that Step 28A owns; the transition table, the HTML sanitiser, path normalisation, the block validator and the rule evaluator are unit-tested under sprint rule 1 — and caught a real defect, a UNC-path misparse that made `//sale//summer//` normalise to `/summer`. Everything else is deferred to Step 29 | — |
| 2026-09-06 | 21 | **`03-database-design.md` §4.15 and §4.18 (both rewritten), `04-api-specification.md` §3.6 (rewritten) and §4, `02-domain-model.md` §2 and §6, `docs/adr/ADR-021` (new)** | **Fifth specification change since Step 0, and the first that *removes* something the specification asked for.** §4.18 named seven `mv_*` materialised views and none was built: a materialised view named `reporting.mv_daily_sales` has to select from `orders`, `payments` and `catalog`, which §1 and `01-architecture.md` §2.1 forbid outright — and because it is declared in DDL rather than in C#, the architecture tests would not catch it, which makes it a worse form of the violation the rule was written to prevent. **ADR-021** replaces the views with seven fact tables written by integration-event handlers, one row per transactional row and denormalised at write time, and records the four rejected alternatives along with the three consequences accepted (no back-fill, an apportioned daily commission figure, and a funnel that starts at the basket). §4.18 rewritten to describe the schema as built. §4.15 was five table names in four lines and is now the columns, the constraints and the indexes: `review_votes`, `product_ratings`, `vendor_ratings` and `abuse_reports` are four tables the sketch did not name and each is load-bearing — a vote is a row keyed on the voter rather than a counter, the histogram is stored because a product page renders it on every load, and a complaint is a row rather than a flag so that ten people reporting one review is ten complaints. **§3.6 was five lines and is now the 24 storefront routes**, with the reason the review write takes an `orderLineId` and ignores the product in its own route, and the reason two of its endpoints are anonymous *writes*. §4 gains the eleven reporting and ten moderation routes. **§6 corrects the Reviews row**: it named one event and there are three, because a review being published and an average moving are different facts with different causes — the average also moves on a rejection, an edit and an upheld complaint. §2's Reviews and Reporting rows restated | — |
| 2026-09-06 | 21 | `IMPLEMENTATION_PLAN.md`, `.env.example`, `src/backend/KlaraHome.sln`, `src/backend/host/*.csproj`, `src/backend/host/ModuleAssemblies.cs`, `src/backend/host/KlaraHome.Api/appsettings.json`, `src/backend/host/KlaraHome.Worker/appsettings.json`, `src/backend/shared/KlaraHome.Contracts/Orders/IOrderPurchases.cs` (new), `src/backend/shared/KlaraHome.Contracts/Inventory/IInventoryAgeing.cs` (new), `src/backend/shared/KlaraHome.Contracts/Reviews/ReviewEvents.cs` (new), `src/backend/shared/KlaraHome.Contracts/Notifications/NotificationEvents.cs`, `src/backend/modules/KlaraHome.Modules.Reviews/**` (new), `src/backend/modules/KlaraHome.Modules.Reporting/**` (new), `src/backend/modules/KlaraHome.Modules.Orders/**`, `src/backend/modules/KlaraHome.Modules.Inventory/**`, `src/backend/modules/KlaraHome.Modules.Catalog/**`, `src/backend/modules/KlaraHome.Modules.Vendors/**`, `src/backend/modules/KlaraHome.Modules.Search/**`, `src/backend/modules/KlaraHome.Modules.Notifications/**`, `src/backend/modules/KlaraHome.Modules.Identity/Infrastructure/Seeding/PermissionCatalog.cs`, `src/backend/tests/KlaraHome.UnitTests/**`, `tools/ef.ps1`, `tools/ef.sh` | Step 21 closed as DONE, and Phase E with it. **Two modules built.** *Reviews* (`reviews` schema, ten tables): verified-purchase reviews whose one-per-line rule is a unique index rather than a handler check; a vote as a row keyed on the voter, which is the whole anti-stuffing mechanism; product and vendor rating aggregates recomputed in full on every change and published as two events carrying the aggregate rather than a delta; Q&A whose `author_type` comes from the caller's claims and never from the request; an abuse queue in which a complaint does not hide anything and upholding it is what does; wishlists that store no price because they are priced from the buy box at read time; and back-in-stock and price-drop alerts that are Marketing rather than transactional, because interest is not consent. *Reporting* (`reporting` schema, nine tables): seven fact tables written by fourteen event subscriptions across six modules, thirteen declared reports served as data the admin app reads back, and scheduled CSV exports stored in the private bucket and emailed as a short-lived signed link. **Two new shared seams** — `IOrderPurchases`, the narrowest of ordering's five and the only one that cannot write, and `IInventoryAgeing`, which exists because ageing is the one inventory number no event carries. **Three new integration events**, and Catalog, Vendors and Search each gained the consumer that finally makes `ProductProjection.RatingAverage` something other than null — the gap Steps 19 and 20 both recorded. **35 endpoints, five permissions, eight feature flags, three background workers.** Four notification event keys and their templates added. Build clean at **0 warnings** in every project this step touched; **941 unit tests** green (31 new), the new ones covering the rating arithmetic, the price-drop rule, the display-name shortening, the schedule's calendar arithmetic including the 31st of February, and the CSV writer's formula-injection defusing. **Twenty-one Parking Lot items and 37 `TEST_DEBT.md` rows recorded**, including both headline acceptance criteria. Nothing proved against a database; everything else is Step 29's | — |
| 2026-09-06 | 22 | **`10-design-system-placeholder.md` §2** | **Sixth specification change since Step 0, and the smallest.** One token added: `--touch-target-min: 44px`. §4 already required touch targets of 44 × 44 px and §2 had no value for it, so every component would have retyped the number - which is the one thing rule 2 of that document forbids. Nothing else in the placeholder token set was touched, and no colour, font, shadow or gradient was added: rule 8 of the execution protocol holds until Step 30 | — |
| 2026-09-06 | 22 | `IMPLEMENTATION_PLAN.md`, `.env.example`, `src/backend/Directory.Packages.props`, `src/backend/host/KlaraHome.Api/KlaraHome.Api.csproj`, `src/backend/modules/KlaraHome.Modules.Search/Infrastructure/SearchOptions.cs`, `tools/generate-api-client.mjs` (new), `tools/generate-api-client.ps1` (new), `tools/ci.ps1`, `.github/workflows/ci.yml`, `src/frontend/**` | Step 22 closed as DONE. **The OpenAPI document is now a build artefact.** `Microsoft.Extensions.ApiDescription.Server` makes `dotnet build` export it (opt-in, `PrivateAssets="all"`); `tools/generate-api-client.mjs` - ~470 lines, no dependencies, and it **throws rather than guesses** on any construct outside the narrow subset ASP.NET Core emits - turns it into 21 injectable clients over **489 operations and 494 models**; and `ci.ps1 -Stage codegen` fails the build if the committed client has drifted, which is what makes a breaking API change impossible to merge unnoticed. Off-the-shelf generators were rejected: one needs a JVM the frontend toolchain has not got, and the rest impose an HTTP abstraction this project has already made a different decision about. **Five interceptors in the one order that works** - loading outermost so the progress bar spans the retries, retry above error normalisation so a retried-then-successful call never toasts its first attempt, the correlation id below retry so every attempt shares an id in the server's log, auth innermost because it alone replays a request. Retry is decided by the HTTP method **and by an `Idempotency-Key`**, never by the URL. The refresh is **single-flight**, without which five concurrent 401s become five racing rotations that the server correctly reads as a stolen token. The access token is held in memory only. Configuration is read **before the injector exists**, so `API_BASE_URL` is a real DI token and one image serves every environment - the `KH_*` variables and the `KH_FEATURE_<key>` encoding are documented in `.env.example`. Also: placeholder tokens and the a11y baseline as SCSS, `en-IN` locale and five pipes, framework-free `Money` and the Indian format validators, and an MSW harness whose own test proves a generated call end to end through the whole chain. **One backend fix made inline rather than parked, because it blocked the deliverable outright:** `SearchOptions.BaseUrl` carried `[Url]`, which rejects the empty string that `.env.example` ships and ADR-019 makes the normal case - so the host failed options validation on start-up. Workspace configuration changed in four places, each recorded in the Parking Lot: `domain` is now a true leaf and `util` may depend on it; `testing` has its own tag plus a **verified** `no-restricted-imports` rule keeping MSW out of production code; `jest.preset.js` gained the three settings MSW and Angular's ESM locale data require; `printWidth` set to 110. Both apps build - storefront **81.6 kB** gzipped, admin **71.0 kB**, against a 180 kB budget; 17 lint and 15 test projects green; backend clean at 0 warnings with **941** unit tests. **Ten Parking Lot items and 18 `TEST_DEBT.md` rows recorded**, including all three headline acceptance criteria and the performance budgets, which are deferred rather than encoded in the wrong unit | — |
| 2026-09-07 | 28B | **`04-api-specification.md` §4 and §7** | **Seventh specification change since Step 0, and entirely documentary.** §4 gains the routes Step 28B built — impersonation start and end, `GET /admin/settings/schema`, `POST /admin/products/bulk-status`, the product-import template's JSON shape, the collection item add and remove, the commission-invoice trio, the warehouse-scoped fulfilment queue and pick list, and the shipment label's move from a streamed PDF to a short-lived signed URL — and records that `GET /admin/reports/{key}` is now two operations because one status code cannot carry two bodies. §7 records that query parameters are declared in `camelCase` by a document transformer. **One deliberate non-addition is written down too**: reference data is *not* mapped again under `/admin`, and the reason is stated where the next person will look for it. No ADR — documenting routes a named deliverable required is routine | — |
| 2026-09-07 | 28B | `IMPLEMENTATION_PLAN.md`, `PARKING_LOT.md`, `TEST_DEBT.md`, `steps/step-28B-deferred-functional-gaps.md` | Step 28B closed. **The Parking Lot was swept a second time and given a state vocabulary**: 443 rows, every open one now carrying an owner that is a step which has not run, `Backlog (post-MVP)`, or — for exactly one row — an explicit `NEEDS A STEP`. 103 rows had no owner among the remaining steps, most of them features correctly deferred into a phase that contains only hardening steps; routing those to a hardening step would have put a false owner on a true row, so a `📋 Backlog` class was added instead and the honest thing recorded. 26 rows added to `TEST_DEBT.md`. No specification change | — |
